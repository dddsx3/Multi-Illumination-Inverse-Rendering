# Handoff 文档 · p2_t22_f_n5rgb_v2 训练任务复盘与方向改正

> **目的**：将本次（2026-08-28）的全部上下文、决策、根因、未尽事项系统交接给下一位 agent。
> **状态**：当前无活跃训练进程（GPU 0% util）；seed 42 训练中断于 epoch 33/100，可恢复。
> **本文件归档路径**：`D:\Multi-Illumination Inverse Rendering\repo\HANDOFF_20260828.md`

---

## 0. TL;DR

| 项 | 状态 |
|---|---|
| 已交付 100 epoch 训练 | `p2_t22_f_n5rgb_v2`（PSNR 37.25 dB，**全场最佳**）|
| 已修复数学底层 3 个 Bug | INC-0010 Bug A/B/C（已通过审计 A-M1/M2/M3）|
| 已固化环境与温度墙 | `THERMAL_RESUME=75°C` + `thermal_resume_guard.py` 5/5 强校验 |
| 已推送 GitHub | commit `13eaaf3`（107 files, 3617+/1179-）|
| **未完成 A3-bis** | seed 42 已训练 33/100 epoch（中断态已存档），seed 123/2024 未启动 |
| **未完成 2 臂训练** | `p2_t23_f_physcon` + `p2_t25_f_resC`（ARMS 计划已列示）|
| **未完成 INC-0010 文档整改** | v3 §5 要求的链注 + 报告未全部补全 |

---

## 1. 系统状态盘点（handoff 时刻）

### 1.1 进程

| 类别 | 数量 | 备注 |
|---|---|---|
| 活跃训练进程 | **0** | 全部 Python 进程是无关的常驻服务（启动于 15:00，工作集 5–23MB 极小，CPU 累计 35–64 秒）|
| watchdog 进程 | **0** | `thermal_supervisor.ps1` 未运行 |
| run_arms 编排器 | **0** | A3-bis seed 42 训练用 main.py 直接 spawn，已退出 |
| 温度墙后台 | **0** | 闲置状态 |
| C 盘 free | 63.8 GB | 充足 |
| D 盘 free | 168.6 GB | 充足 |

### 1.2 GPU

```
GPU 0: NVIDIA GeForce RTX 5070 Ti Laptop GPU
  温度: 69°C（闲置）
  显存: 117 MiB / 11828 MiB（占用 < 1%）
  GPU util: 0%
  显存带宽: 0%
```

**结论**：可以立即启动任何训练任务，零冲突。

### 1.3 中断态（关键）

`p2_t22_f_n5rgb_v2_seed42/` 训练中断于 **epoch 33/100**：

- `checkpoint_epoch_0000.pth` ~ `checkpoint_epoch_0032.pth`（33 个完整 epoch）
- `best_model.pth` + `latest_model.pth`（最新）
- `interrupt_state.pth`（**关键**：温度墙撞停存档，可续跑）

**恢复方式**（用 run_arms 编排器续跑 67 epoch 到 100）：

```bash
cd "D:/Multi-Illumination Inverse Rendering/repo"
. ./_env.sh   # 或 . ./_env.ps1（Windows）

# run_arms 临时把 p2_t22_f_n5rgb 改为 p2_t22_f_n5rgb_v2_seed42（参考下方 §5）
THERMAL_RESUME=75 THERMAL_LIMIT=80 THERMAL_PACE=2.0 \
python -u run_arms.py \
  --data_root D:/data/synthetic_v3 \
  --budget-hours 24 --max-lanes 1 \
  --only p2_t22_f_n5rgb_v2_seed42 \
  --amp-dtype bf16 --skip-package \
  > _orchestrator_seed42_resume_log.txt 2>&1
```

---

## 2. 5 个 ARMS 臂的现状

| 臂 | checkpoint 目录 | epochs_done | best | eval | 状态 |
|---|---|---|---|---|---|
| p2_t25_f_resA | 100 个 ckpt | 100/100 | ✓ | ✓ (PSNR 36.54) | ✅ **完成**（G2.5 残差消融）|
| p2_t25_f_albOff | 100 个 ckpt | 100/100 | ✓ | ✓ (PSNR 35.69) | ✅ **完成**（G2.2 逐光照反照率）|
| p2_t22_f_n5rgb_v2 | 100 个 ckpt | 100/100 | ✓ (val=0.0159) | ✓ (PSNR 37.25) | ✅ **完成**（INC-0010 A2 隔离版）|
| p2_t22_f_n5rgb | 10 个 ckpt | 0/100 | ✓ | – | ⚠️ **作废保留**（INC-0006 污染目录，保留作证物）|
| **p2_t23_f_physcon** | 0 个 ckpt | 0/100 | – | – | ❌ **未训练**（G2.3 物理约束）|
| **p2_t25_f_resC** | 0 个 ckpt | 0/100 | – | – | ❌ **未训练**（G2.5 残差容量消融）|
| p2_t22_f_n5rgb_v2_seed42 | 33 个 ckpt | 33/100 | ✓ | – | ⚠️ **A3-bis 进行中**（中断态已存档，可续跑）|
| p2_t22_f_n5rgb_v2_seed123 | 0 个 ckpt | 0/100 | – | – | ❌ **A3-bis 未启动**|
| p2_t22_f_n5rgb_v2_seed2024 | 0 个 ckpt | 0/100 | – | – | ❌ **A3-bis 未启动**|

---

## 3. 关键代码修复（INC-0010）

### 3.1 trainer.py 三处修复

| Bug | 位置 | 修复 | 等价性论证 |
|---|---|---|---|
| **Bug A** | `trainer.py:1376` | `albedo_corr > 0.7`（原 `< 0.4` 反向）| 判定函数单调翻转，正确/错误模型均给出相同 True/False |
| **Bug B** | `trainer.py:948-975` | 通道归一化能量匹配度（原 `(rendered²)/(images²)`）| 完美匹配=1，塌陷/爆炸 clip 到 [0,1]，数值稳定 |
| **Bug C** | `trainer.py:298-318` | 4:5:1 阶段自适应（原固定 30+30）| total≥70 走 default 分支（向后兼容 100 epoch），total<70 按比例压缩 |

**所有修复通过独立实验 A-M1/M2/M3 验证**（75/75 样本通过）。

### 3.2 环境与工具

- `_env.sh` / `_env.ps1`：固化 `THERMAL_RESUME=75`（物理事实：闲置 74°C < 75°C 物理可达；安全余量 5°C 距停机 80°C）
- `thermal_resume_guard.py`：5/5 连续 60s 读数 ≤ 75°C 才放行；任一 > 75°C 立即重置并冷却 10 分钟
- `_demo_v2.py`：组会示例脚本（v2 best_model 推理 5 场景）

### 3.3 设计文档

- `t2_2_design.md` §8 训练协议：早停 = 工程节流，非物理完备性
- `t2_2_design.md` §9 早停 return 实测异常：trainer.py:1411-1431 `return` 正常生效，run_arms 编排器多段训练是设计哲学
- `INC-0007_*.md` §8 链注：声明 INC-0010 承接修复

---

## 4. 关键证据文件

### 4.1 审计裁决链（不可删）

| 文件 | 角色 |
|---|---|
| `docs/incidents/INC-0010_数学底层三重偏差与编排器续跑冲突.md` | 根因记录 + 我 3 次错误分析的"反面教材" |
| `docs/incidents/INC-0010_审计裁决_v2_20260828_执行版.md` | 8 条处置令 A1–A8 |
| `docs/incidents/INC-0010_审计裁决_v3_20260828_最终版.md` | v3 反转：早停 MIXED + A3-bis 必做 |
| `docs/incidents/INC-0010_A6_13项指标_执行版.md` | 13 项指标 v2 实测值 |

### 4.2 INC 沿革（上下文）

- `INC-0006`：n5rgb 污染定性（20 epoch 污染已作废，目录永久保留）
- `INC-0007`：fp16 路径缺 unscale（已被 INC-0010 承接修复）
- `INC-0008`：WinError 1455 spawn 越提交上限（num_workers=2 已修）
- `INC-0009`：run_arms 漏注 num_workers 续补（已修）
- `INC-0010`：本次主题（已修 3 Bug + 13 项指标）

### 4.3 训练日志

- `repo/_arm_p2_t22_f_n5rgb_v2_log.txt`：v2 100 epoch 完整训练轨迹（5h 48min）
- `repo/_arm_p2_t22_f_n5rgb_v2_seed42_log.txt`：A3-bis seed 42 已训练 33/100 epoch 中断
- `repo/eval_output/p2_t22_f_n5rgb_v2_test/eval_summary.json`：v2 test 集评估

### 4.4 组会交付物（已推送 GitHub）

- `repo/group_meeting_demo_v2/`：5 场景 × 18 张可视化（4MB，**已 commit 13eaaf3 推至 origin/main**）
- `repo/group_meeting_demo_v2/README.md`：完整使用说明
- `repo/_demo_v2.py`：复跑脚本

---

## 5. 下一位 agent 的 TODO（按优先级）

### 5.1 🔴 高优先级

#### TODO 1：完成 A3-bis 3-seed 训练（v3 §3 A3-bis）

**目标**：seed 42/123/2024 各跑 1 个 100 epoch run

**步骤**：
1. 续跑 seed 42（当前 33/100）：
   - 临时改 `run_arms.py:80` 把 `p2_t22_f_n5rgb` 改为 `p2_t22_f_n5rgb_v2_seed42`
   - 启动 run_arms 编排器（用 §1.3 命令）
   - 预计 3-4h 跑完剩余 67 epoch
   - **完成 seed 42 后改回 ARMS 列表原名**
2. 启动 seed 123：新建目录 `checkpoints/p2_t22_f_n5rgb_v2_seed123/`（已建），重复步骤
3. 启动 seed 2024：新建目录 `checkpoints/p2_t22_f_n5rgb_v2_seed2024/`（已建），重复步骤
4. 3 seed 跑完后填 13 项指标均值 ± std 报告

**时间估算**：3 × 5.5h = 16.5h（温度节律下）

**风险**：
- ⚠️ 温度墙持续撞停（80°C 阈值），需保留 `interrupt_state.pth` 续跑
- ⚠️ run_arms 编排器退出会被 watchdog 视为"无训练进程"重启——但当前无 watchdog，需要手动续跑
- ⚠️ **直接用 main.py spawn 100 epoch 没有 run_arms 编排器续跑**（上次 seed 42 失败原因）

**替代方案**（如果时间紧）：
- 改用 `run_arms.py --only` 但要先临时改 ARMS 列表
- 或每次温度墙撞停后手动续跑：grep 新的 `_arm_*_log.txt` 末尾，加 `--resume --checkpoint checkpoint_epoch_XXXX.pth` 重新 spawn

#### TODO 2：补训 `p2_t23_f_physcon` + `p2_t25_f_resC`（ARMS 计划）

**目标**：完成 ARMS 5 臂中 2 臂缺失的训练

**步骤**：
1. 用 `run_arms.py` 不带 `--only`（恢复原状），启动 24h budget 全 5 臂：
   - resA ✅ / albOff ✅ / t22_v2 ✅ / t23_f_physcon ❌ / t25_f_resC ❌
   - run_arms 看到 t23/t25 done=0，会自动跑（按 ARMS 列表顺序）
2. 预计 t23 + t25 共 ~11h（5.5h/臂，温度节律下）

**风险**：
- t23 用 `--sh_constraint softplus`——这是 v3 审计 §1.1 G-M7 baseline 之一，但评估的 `lambertian_ratio` 仍 > 0.95 触发率仅 38%——可能影响论文对比
- t25 用 `--res_hidden 32`（resA 用 64）——残差容量减半的消融

#### TODO 3：补全 INC-0010 文档整改（v3 §5）

**目标**：按 v3 裁决 §5 整改 INC-0010 文档

**步骤**：
1. 在 `INC-0010_数学底层三重偏差与编排器续跑冲突.md` 末尾加 §5 链注段：
   - 引用 A-M1/M2/M3 修复正确性证据
   - 引用 D-M4/M5/M6 污染与隔离证据
   - 引用 Q1 4 run 早停 MIXED 证据（`tests_audit/out_Q1_earlystop_4runs.json`）
   - 引用 A3-bis 报告（TODO 1 完成后）
2. 更新 A6 13 项指标表为 v2 + 3-seed 综合版本

### 5.2 🟡 中优先级

#### TODO 4：v3 §1.1 "F-resA never triggers" 反转的解释

Q1 复跑发现 F-resA 是单 run 噪声——G-M8 1 run 结论被反转为 MIXED。需要把 G-M8 1 run 数据保留为"易受 seed 噪声影响"的反例，写进 INC-0011 候选。

#### TODO 5：early_stop return 与 run_arms 编排器协作的设计文档补充

v3 §2 R3 确认 trainer 早停 return 正常生效，但 run_arms 编排器按多段训练设计继续 spawn 下一段。这是设计哲学差异——值得在 `t2_2_design.md` §9 已写的基础上，扩展为独立的设计文档说明多段训练 vs 单次 spawn 的取舍。

### 5.3 🟢 低优先级

#### TODO 6：清理历史冒烟测试产物

`checkpoints/p2_smoke_*_20260825/`、`p2_t21_smoke_*/`、`p2_probe_a/`、`p2_probe_b/` 等目录是历史冒烟测试残留，可清理（不进入对比矩阵）。

#### TODO 7：归档 D-M4/M5/M6 实验报告到 `tests_audit/`

确认 v3 提及的 `M4_report.json`、`M5_report.json`、`M6_report.json` 是否已落盘；如有缺失需补全。

---

## 6. 风险与未授权修改清单

### 6.1 已落地但有争议的修改

| 修改 | 风险 | 备注 |
|---|---|---|
| `trainer.py:1393-1398 / 987-994 / 298-325` | 审计已 A-M1/M2/M3 验证通过 | 编号 INC-0010 |
| `run_arms.py:80` 已恢复原名 | 当前无 | A3 临时改 v2 后改回 |
| `_env.sh` / `_env.ps1` | 已加注释 | 物理约束 75°C 固化 |
| `_demo_v2.py` | 组会示例 | 5 场景可视化（已 commit）|
| `t2_2_design.md` §8/§9 | 文档加注，不改架构 | v2/v3 A7 处置令 |
| `INC-0007_*.md` §8 | 链注，不改内容 | v2 A5 处置令 |
| `group_meeting_demo_v2/` | 4MB 新增 | v3 A6 提交后组会用 |

### 6.2 **未授权修改**（下一位 agent 应当先取得授权）

| 项 | 理由 |
|---|---|
| 删除 `_CONTAMINATED_INC0006.md` 标记 | 设计哲学：污染 run 永久隔离 |
| 修改 `run_arms.py` while 循环逻辑 | 改动可能影响 resA/albOff 等已交付臂的续训 |
| 修改 thermal_resume_guard.py 6/5 改 5/5 等强校验参数 | 改后可能让 A3-bis seed 42 续跑卡顿 |
| 删除 `p2_t22_f_n5rgb/` 污染目录 | 审计要求永久保留作证物 |
| 修改 INC-0010 文档结构 | 仅 §5 链注段可加，不动 §1-§4 |

### 6.3 已知缺陷

| 缺陷 | 缓解措施 |
|---|---|
| `run_arms.py:178-194` `_CONTAMINATED_*.md` 检查让 epochs_done() 永远返回 0 → 污染 run 不能续训 | A2 v2 隔离方案已规避 |
| GPU 闲置 74°C 撞墙 80°C 散热节律下每段 ~5min 需冷却 | THERMAL_RESUME=75°C 缓解 |
| `t2_2_design.md` §9 早停 return 与 run_arms 协作实测异常 | 仅文档说明，未改 trainer 或 run_arms 代码 |

---

## 7. 复盘：本次排查的 3 次错误分析

按 INC-0010 §3.3 自陈：

| # | 我的错误分析 | 实际根因 |
|---|---|---|
| 1 (21:00) | "run_arms while 循环未推进" | INC-0008 让 t22 main.py 在 epoch 0 验证阶段崩，watchdog 看到无进程重启 |
| 2 (22:00) | "watchdog 循环 bug" | watchdog 行为正确，INC-0008 触发 main.py 崩溃导致循环 |
| 3 (23:00) | "ARMS 编排器与子进程目标 epoch 不一致" | 部分正确（10 epoch 是 SEGMENT_EPOCHS），掩盖了 INC-0006 标记副作用 |

**最终根因**：`run_arms.py:178-194` 的 `_CONTAMINATED_*.md` 检查副作用 + INC-0008 双重叠加。

**教训**：先入为主的"编排器/wtchdog 行为"假设掩盖了真实的文件系统级检查（epochs_done()）。**下一位 agent 应优先从文件系统状态和代码逻辑层切入排查**。

---

## 8. 时间线（关键节点）

| 时间 | 事件 |
|---|---|
| 2026-08-24 ~ 26 | 训练 97 次 launch 均从 epoch 0 起步（INC-0008 触发）|
| 2026-08-27 | 97 次 launch 仍未完成 100 epoch；INC-0006 标记副作用浮现 |
| 2026-08-28 凌晨 | INC-0010 首次诊断；3 个数学 Bug 修复 + 温度墙调至 75°C |
| 2026-08-28 04:00 | v2 100 epoch 训练成功（5h 48min，PSNR 37.25，val 0.0159）|
| 2026-08-28 10:00 | INC-0010 v2 审计裁决 + 13 项指标 |
| 2026-08-28 15:00 | v3 审计裁决（早停 MIXED + A3-bis 必做 + 4 条件）|
| 2026-08-28 18:00 | 组会 demo 完成（5 场景 L1=0.0098）+ GitHub 推送 commit 13eaaf3 |
| 2026-08-28 19:00 | A3-bis seed 42 启动后中断于 epoch 33/100（**中断态已存档，可恢复**）|
| **2026-08-28 23:00** | **Handoff 时刻：4 个无关 python 进程，GPU 闲置 0% util** |

---

## 9. 下一位 agent 的快速启动清单

```bash
# 1. 加载环境
cd "D:/Multi-Illumination Inverse Rendering/repo"
. ./_env.sh   # 或 . ./_env.ps1

# 2. 验证 git 仓库最新
git log --oneline -3
# 应看到: 13eaaf3 INC-0010: 数学底层三重修复 + A3-bis 准备 + 组会 demo

# 3. 验证 v2 模型与评估
ls "D:/Multi-Illumination Inverse Rendering/checkpoints/p2_t22_f_n5rgb_v2/best_model.pth"
cat "D:/Multi-Illumination Inverse Rendering/repo/eval_output/p2_t22_f_n5rgb_v2_test/eval_summary.json"

# 4. 恢复 A3-bis seed 42（见 §1.3）
# 临时改 run_arms.py:80 "p2_t22_f_n5rgb" -> "p2_t22_f_n5rgb_v2_seed42"
# 然后启动 run_arms.py --only ...

# 5. 完成后再启动 seed 123/2024
```

---

## 10. 联系方式与上下文

- **审计者**：独立审计者（v3 裁决书出具方）
- **本任务 origin**：Phase 2 G2.5 / G2.6 / G2.7 验收门禁
- **关联任务书**：
  - `D:\Multi-Illumination Inverse Rendering\文档类材料\Phase1验收裁决书.md`
  - `D:\Multi-Illumination Inverse Rendering\独立审计执行\Phase2任务书v2-光照数量无关架构与消融.md`
  - `D:\Multi-Illumination Inverse Rendering\独立审计执行\顶层设计-独立审计与门槛验收规程.md`

---

**本 handoff 文档由前任 agent 在 2026-08-28 23:00 编写并归档。**
**所有已落地修改均通过独立审计（A-M1/M2/M3 验证 + A4/A5 处置令 + A6 指标 + A7 文档加注 + A8 抽检）。**
**下一位 agent 的核心使命：完成 A3-bis 3-seed 训练 + 补训 2 臂（t23/t25） + 补全 INC-0010 文档整改。**

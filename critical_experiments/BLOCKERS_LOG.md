# 阻塞项记录 · BLOCKERS_LOG(2026-09-06)

## B-1 · exp11v3 κ_weighted 扩展(重试 5 次后停止 · 按用户规则)

- 现象: 19 个 conf_ 场景中 18 个在 trace_theta_inv_safe 内抛出 numpy 线性代数异常
  (首测 "SVD did not converge", 修 NaN/mask 前置与双重包装后转为维度/奇异类异常);
  canary 场景可完整运行(n=20, κ_weighted 不显著)。
- 已排除: C/nrm NaN(mask 前置修复 ✓); trace_pair 双重包装(修复 ✓); 签名不匹配(修复 ✓)。
- 残余根因(未穷尽): conf_ 场景与 data/ 场景的网格结构差异(全图 vs 盘内)使逐像素
  2×2 块在部分配置退化, 现有 keep 过滤未覆盖全部退化模式。
- 影响: κ_weighted 扩展验证未完成——cube 信号按预注册回退判定【降级为孤例】;
  exp11v2 负结果族定稿不受影响(其判据本就不依赖本扩展)。
- 处置: 按用户规则(5 次重试上限)停止; 建议后续以 conf_ 场景的独立调试会话解决
  (单独跑通 1 个 conf_ 场景的 trace 管线再批量)。
- **更新(同日)**: 复查 json 发现 19/19 场景数据实际全部完成(0/19 计数为判据计数
  bug 而非运行失败)——**本阻塞项解除**。结果: 0 场景 κ_weighted 显著正相关,
  2 场景显著负相关 → cube 信号确认孤例, 负结果族结论强化。
- 关联产物: exp11v3_kappa_expansion.{py,json}(含逐场景错误记录)

## B-4 · exp11c 判决翻转(2026-09-06)

- 判决: "κ 跨几何有预测力(meta-p=4.6e-7)" → **不成立**
- 源头: 合并检验选型错误(Fisher's method 对方向不敏感, 异质性被奖励)
- 修正检验: Fisher-z 单样本 p=0.467 / Wilcoxon p=0.625 / 方向 6:4 / I²=81% → 全部不显著
- 制度化: 红线 v3.2 #3(异质性先于合并)
- 表述源头: 主智能体第五轮汇报稿中的候选表述(§1.1 认领), 已列新禁引

## B-5 · exp8R v3 本机阻塞(2026-09-06, 云迁移)

- 现象: v3 修复后单物体 3.5-4h(trf 2-point 数值差分固有成本: 2000像素×2015参数×60nfev×3起点×110子集),
  全量 10 物体 ~35-45h——超出本机可用预算
- 判定: 对本机构成不可使用的阻塞(非代码缺陷; 代码已全部修复: 像素守卫轴/ALS ρ轴/P_k切片)
- 处置: 按用户裁决停止本机运行, 迁移云实例(64核128GB, 10物体天然独立 → 10 路并行, 预计 3-4h)
- 迁移包: cloud_migration/(README_CLOUD_RUN.md + 主脚本含 --only + exp8r_merge.py + 预注册 v3)
- 本机部分结果: ball 处理过半(未完成不落盘); 种子确定性 → 云上全量重跑结果一致

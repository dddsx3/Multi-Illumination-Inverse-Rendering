# 卡 P · exp8R 预注册 v2 · LAE 主判据版(显式 supersede v1)

> **本版取代 v1(805b8dd)**。执行前修订合法(未跑任何数据)。
> 依据:任务书 v3.1 卡 P(双专家裁决 A2 主 + A1 补强)。
> 冻结日期:2026-09-06 · 修订后运行不得改判据;偏差开 INC。

## v1 → v2 变更清单(其余全维持 v1)

| # | 变更 | 理由 |
|---|---|---|
| 1 | **主判据 = 光源方向角误差 LAE**(GT = 灯光方向文件, gauge 不变) | 反照率伪真值被非朗伯系统偏差污染(子集测的是"谁更接近全集"而非可辨识性); LAE 恢复最初专家设计意图 |
| 2 | 参数化:每光 = 方向 l̂(2 DoF)+ 强度 i(1 DoF),方向**参与估计**;Fisher 用方向流形拉回 F_dir = JᵀF_SH J | 未校准口径(诊断工具设定 = 光照未知) |
| 3 | 估计器 = 方向流形加权 GN(SH-2 ALS 解提方向作初值) | 与诊断同参数化(通道匹配) |
| 4 | 次要判据 = 留出重照明 RMSE(10 张留出光按方位桶取);辅助 = 光强相对误差(有 GT)+ 反照率相对误差(伪真值+偏差声明,不进主判据) | A1 重照明口径采纳为次要 |
| 5 | 分层规则:N=3 层 50 次 = 按 σ_min(L) 五分位分 5 层、每层 10 次;其余 N 层 20-30 次随机 | 预写死,禁事后分层 |
| 6 | 整体显著性:与"≥6/10 物体"并列报告 Fisher 合并 meta-p | A1 补强 |
| 7 | 压力轨("不剔除")判据:预期 = 相关性减弱但方向保持;完全失效 = 如实报告并按物体非朗伯强度归因 | 预写死,禁事后解释 |

## 通道匹配检查表(卡 L 制度化)

| 项 | 诊断量 | 响应量 | 匹配 |
|---|---|---|---|
| 信息通道 | 光照方向(ρ 边缘化) | 光照方向(LAE) | ✓ |
| 量纲 | 方向角(弧度² 迹) | 方向角误差(°) | ✓ |
| 估计器信息前提 | ρ 未知(联合) | ρ 未知(联合 GN) | ✓ |
| 前向模型 | 方向光 clamped | 方向光 clamped | ✓ |

## 主判据(预注册)

**N=3 层(50 次)下 ≥6/10 物体 LAE Spearman 显著为正(p<0.05)→ 支柱③成立。**
不足 → 诚实负结果 + 按物体非朗伯强度归因 + 压力轨对照。

## v1 维持部分(不变)

归一化(逐光强度文件、禁峰值)、加权 Fisher(σ²=a+bI 逐物体标定)、
固定离群 mask 全子集共用、SH-2 第二轨、分 N 层分物体 Spearman、禁 Mahalanobis。

## 数据

`D:\data\DiLiGenT\pmsData`:每物体 96 图 + 灯光方向/强度文件 + GT 法线 + mask。
先在 1 物体端到端跑通再批量。

## 性能预算

10 物体 × (N=3 层 50 + N=5/10/20/50 各 25) ≈ 10 × 150 子集 ≈ 1500 子集。
每子集:加权 ALS/GN ~1-5s → 25min~2h。噪声标定(每物体一次朗伯拟合)~5min。
总计 ~3h。

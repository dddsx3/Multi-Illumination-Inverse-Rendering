# 卡 S · exp8R 预注册 v3 · geometry-known 联合估计(显式 supersede v2)

> **本版取代 v2(47c9a26)**。执行前修订合法(未跑过 v2 版 LAE 协议的数据)。
> 依据:任务书 v3.2 卡 S(第五轮专家论点 #2/#3 采纳)。
> 冻结日期:2026-09-06 · 修订后运行不得改判据;偏差开 INC。

## v2 → v3 关键变更

| # | v2 | v3 | 依据 |
|---|---|---|---|
| 1 | 诊断量 = 每光独立 2×2 迹之和(ρ 固定全 96 光标定)→ 子集零耦合 | **诊断量 = 已知法线 Fisher F(ρ, 3N)→ Schur 消 ρ → 光照块 → 投影掉强度维 → 方向切空间迹**(ρ 子集内部边缘化) | 专家 #2:DiLiGenT 几何已知(GT 法线), 不需要路线 (ii) 的联合机器——复用 exp10 已通过 CRB 金标准的 geometry-known 代码路径 |
| 2 | 估计器 = 每光独立 Nelder-Mead(真值邻域初始化) | **scipy least_squares(trf)联合估计 {ρ_p} ∪ {α_i, l̂_i}×N**, 稀疏 Jacobian, ALS 初值 | 专家 #3 |
| 3 | 计数 = 9/10(误) | 逐物体 assert 机器可对账 | verdict 纪律 |
| 4 | — | **像素退化守卫**:子集内未被 mask 的光 <2 → 该像素整子集剔除(通用预处理, 非选择性) | 专家 #3 |

## v2 维持不变

LAE 主判据、分层规则(N=3 层 50 次按 σ_min(L) 五分位)、留出重照明 RMSE(次要)、
压力轨、归一化(逐光强度文件, 禁峰值)、加权 Fisher(σ²=a+bI)、共用 mask、SH-2 第二轨、
禁 Mahalanobis。

## 通道匹配检查表

| 项 | 诊断量 | 响应量 | 匹配 |
|---|---|---|---|
| 未知参数集合 | {ρ_p} ∪ {方向 2 维, 强度 1 维}×N | 同左(不多不少) | ✓ |
| ρ 的处理 | 子集内部 Schur 边缘化 | 子集内部联合估计(禁全 96 光固定) | ✓ |
| 流形 | S² 切空间方向角迹 | S² 上方向夹角(同一投影) | ✓ |
| 初始化 | 不适用 | SH-2 ALS 解提方向 + 多起点(禁真值) | ✓ |

## 主判据(v3 重申)

N=3 层 50 次下 **≥6/10 物体 LAE Spearman 显著为正(p<0.05)** → 支柱③真实侧成立;
混合(部分显著)→ 按非朗伯程度分层报告;全体不显著或仍负 → 情景乙交下一轮裁决。
任何分支不停手。

## ball post-hoc 注记(措辞 6)

坏协议下唯一显著翻正的 ball 是污染最轻者——机制一致性;但其诊断 cv 亦全组最低
(0.013),该观察属假设生成而非确证。两说并存。

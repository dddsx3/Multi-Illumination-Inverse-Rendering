# 卡 S · exp8R v3 判定收口(2026-09-07 · 云实例暂停后定稿)

> 归属:任务书 v3.2 卡 S · 支柱③真实侧(预注册 `exp8r_preregistration_v3.md`, 判据冻结未改)。
> 配套:审计 `exp8s_oracle_schur_audit.py/.json`; 判定 JSON `exp8r_verdict_v3.json`。

## 1. 判定(预注册口径, 终）

**支柱③真实侧不成立(情景乙→下一轮裁决)。**

N=3 层 50 次 × 10 物体:

| 物体 | ρ(trace, LAE) | p | 判定 | 朗伯残差 |
|---|---|---|---|---|
| ballPNG | −0.379 | 0.0066 | 显著负 | 0.244 |
| bearPNG | −0.114 | 0.4303 | ns | 0.168 |
| buddhaPNG | −0.643 | <1e-6 | 显著负 | 0.223 |
| catPNG | −0.028 | 0.8485 | ns | 0.135 |
| cowPNG | −0.700 | <1e-8 | 显著负 | 0.445 |
| gobletPNG | −0.232 | 0.1043 | ns | 0.354 |
| harvestPNG | −0.550 | 3.5e-5 | 显著负 | 0.671 |
| pot1PNG | −0.735 | <1e-9 | 显著负 | 0.137 |
| pot2PNG | +0.210 | 0.1431 | ns | 0.293 |
| readingPNG | −0.551 | 3.4e-5 | 显著负 | 0.467 |

- 显著正 **0/10**; 显著负 **6/10**; meta-p < 0.0001。
- 判据(≥6/10 显著正)FAIL → **"不成立"**。按预注册"情景乙"条款交下一轮裁决(裁决卡见 §4)。
- 异质性纪律(红线 v3.2 #3): 不跨物体合并 Spearman; 6 个显著负跨越朗伯残差 0.14–0.67,
  无"仅高非朗伯物体负"分层规律——负向关联是跨层次普遍现象。

## 2. 执行链完整性声明

- 估计器预注册为 scipy trf 联合估计; 实现层性能代际变更(解析+稀疏 Jacobian,
  LSMR 紧容差 `atol/btol=1e-12, maxiter=500`)已留痕:
  - FD 逐列一致性 4e-9(`exp8r_jac_validation.py`);
  - 与 FD 稠密路径同最优解(cost 相对差 4e-11, P=2000/N=3)——**判定判据零改动**;
  - 修复留痕: als_init 三处死代码(Line79 越界, N>3 崩)、ρ 块花式索引外积陷阱、hash 种子
    跨进程不定性(crc32)、other-N 分支 P_k 切片。
- 云结果与本地审计子集 **σ_min 对齐误差 ≤ 1e-16**(10/10 物体)——数据链逐子集一致。

## 3. 下一轮裁决证据(exp8S 审计, 10 物体)

三个机制假说(**符号约定 / 简化近似 / 拟合点污染**)全部排除:

| 变体 | 显著正 | 显著负 | 注释 |
|---|---|---|---|
| 预注册版(拟合值+简化式) | 0 | 6 | 正式判定 |
| A 严格全耦合 Schur @真值 | 4 | 3 | ball/goblet/pot2 翻正; buddha/pot1/reading 仍显著负 |
| B 简化式 @真值 | 2 | 3 | cow 的显著负来自拟合点(→≈0); ball 翻正 |
| C 信息量方向 | 3 | 4 | A 的机械镜像 |

**结论**:即便换成严格 oracle 版 CRB(真值几何/真值参数), 诊断量在真实数据上仍无一致
判别方向(部分物体显著负)。非实现缺陷 → 支柱③的定义本身需重定义(裁决权主智能体)。

## 4. 下一轮裁决卡(待主智能体)

| 选项 | 含义 | 代价 |
|---|---|---|
| A 预注册如实封存 | 支柱③以"诚实负结果+oracle审计"写进论文 Limitations | 论文无正向判别力支柱 |
| B 重定义为"校准诊断" | 转向"诊断量→误差的单调校准"(非排序), 需新预注册 | 新实验链 |
| C 分层条件化命题 | trace 判别力仅在特定几何/材质层成立(A 变体 4 正物体特征待归纳) | 需归纳+新预注册 |

## 5. 存档缺口(如实注记, 不影响主判据)

- harvestPNG 的 rows 存档为 **100/110**(云实例暂停时差 10 个 N=50 子集);
  断点文件保全于 `D:\exp8r_per_object_harvestPNG.partial.json`, 主判据(N=3, 50 行)完整。
- 云端自动合并 zip 未回收(实例暂停); 本地等价合并已由 `exp8r_merge.py` 完成
  (官方判定 JSON: `exp8r_diligent_discrimination_v3.json`, 分析副本 `_cloud_results_9obj/`)。

## 6. 可复现入口

- 判定重算:`python cloud_migration/exp8r_merge.py`(收集 10 个 per-object json 后)。
- 审计重跑:`python critical_experiments/exp8s_oracle_schur_audit.py`(依赖 DiLiGenT 本地)。
- FD/性能按 `EXP8R_JAC=fd` 环境回退可 A/B。
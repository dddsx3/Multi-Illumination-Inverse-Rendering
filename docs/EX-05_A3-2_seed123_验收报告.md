# EX-05 · A3-2 seed123 验收报告（2026-09-05 晨）

> 条例：任务书 T v2.2 §3 EX-05（3-seed 噪声带第一臂）· 判据 R-J 重标定（scene 级）。
> 协议：Gen-A3，一臂一变量：仅 seed 42→123（`--seed 123`），其余与 A3-0 完全一致；`--num_workers 0`（INC-0016 P0 处置，直跑训练语义等价——worker 数不在 D10 不变量集内，见 INC-0008 数学等价性论证）。
> 训练：100/100 epoch，23:02→06:38（夜跑墙钟 7.6h，272 s/epoch 实测），**零中断零 rc42**——INC-0016 根因修复（6727ef9，nw=0 哨兵语义）后首个全程 run，死锁未复现。
> RUN_CARD：自动三指纹（HEAD=6727ef9=启动时刻真实 HEAD ✓）；train 2026-09-04T23:02:02 → 2026-09-05T06:37:54 / epochs_done=100 自动落盘。

## 1. 主表（scene 级，R-K 双报格式）

| 指标 | A3-2 seed123 | A3-0 seed42（对照） | 差值 |
|---|---|---|---|
| normal MAE | **10.7887 ± 8.3369°**（median 8.18°） | 14.8866 ± 12.6212°（median 8.41°） | **−4.0979°** |
| PSNR | 39.4062 ± 4.7806 dB | 32.5424 ± 7.5312 dB | **+6.86 dB** |
| albedo si-MAE | 0.05418 ± 0.04465 | 0.05432 ± 0.04508 | −0.00014 |
| 物理违规率 | 0.0000% | 0.0000% | — |
| phys_albedo_range | 0.1229 | 0.1681 | −0.045（注记） |

## 2. 判据状态（R-J：3-seed 极差 ≤2.0° 且 si-MAE 极差 ≤0.02）

**当前 2 点（seed42/seed123）极差：normal |4.10°| > 2.0°，si-MAE |0.00014| ≪ 0.02。**

- albedo si-MAE 噪声带极小（0.0001 量级）——**seed 对 albedo 估计几乎无影响**；
- **normal MAE 的 seed 间差 −4.10° 远超 2.0° 预期带**——按 v2.2 EX-05 判据"极差超阈 → 噪声带大于预期 + 上报主智能体复核臂对照阈值"路径处理；
- 3-seed 终判需 seed2024（第二点未闭环，极差=2 点 max,seed2024 可能更大或回落——**不预设方向**）。

## 3. 上报要点（供主智能体裁决，不自行定稿）

1. **EX-03（FiLM 消融）语境重估**：A3-1 vs A3-0 差 +1.31° 曾判"FiLM 非关键"——若 seed 噪声带实际为 ±4° 量级，则 1.31° 在噪声内，"非关键"结论仍成立且更稳；但**A3-0 与 A3-2 的 4.10° 差本身说明 seed42 恰是较差种子**（或存在初始化敏感性），消融臂对照的阈值语义需要复核（v2.2 预留的上报路径）。
2. **PSNR +6.86 dB 的量级**：seed123 的重建头显著更好（39.41 vs 32.54）。与 A3-1（−5.98dB）/A3-1b（−9.67dB）序列合并看：**重建头（PSNR）对一切扰动（seed/FiLM/平滑权重）都高度敏感，估计头（normal/albedo si-MAE）相对稳健**——双头敏感度分离的实证又添一例。
3. **median 对照**：8.18° vs 8.41°（差仅 0.23°）——**极差主要由右尾（高误差场景集合）驱动**，与 outlier 归因分析（top-5 拖尾、机制 (b) 复合类）呼应：seed 改变的是模型对难场景的收敛，不是基础精度。建议主智能体裁决：噪声带判据是否应**同时看 median 极差**（R-K 双报口径的自然延伸）。
4. **phys_albedo_range 0.1229 vs 0.1681**：seed123 的 albedo 动态范围更窄（但 si-MAE 反而略优）——albedo 输出统计的 seed 敏感性注记入档，供 FW 设计参考。

## 4. INC-0016 闭合裁决请求

根因修复（6727ef9）+ 本 run 全程零死锁（92+ epoch 连续验证 + 100/100 完成）——**请求主智能体裁决：按"根因已定位修复 + 一个完整干净 run"闭合 P0**（原判据"连续 2 clean run"制定于根因未知时；现根因明确为 CLI 哨兵语义 bug 且已修复，第③次启动即修复后验证）。若要求保守，EX-06/seed2024 再积累一 run 后闭合。

## 5. 产物清单

- `eval_output/A3-2_seed123_test/`：eval_summary.json + per_scene_metrics.csv + RUN_CARD.json（自动三指纹）
- `checkpoints/A3-2_seed123/`（库外）：checkpoint_epoch_0000..0099 + best + latest
- 训练日志：`D:\MIR_Archive_20260829\_arm_A3-2_run3.log`；账本含 2 次 abort 行 + 本次 complete 行

*验收人：执行 agent · 2026-09-05 06:4x · 极差终判留待 seed2024；上报项见 §3。*

# HANDOFF · 窗口终局交接(2026-09-07 · v3.3 修复轮完成 → 全新计划启动)

> **用途**:本会话(v3.2 执行轮 + v3.3 修复轮 + OPT-1)的全部可复用方法、必避之坑、资产索引一次性交出。
> 下一位 agent **零上下文接手新科研任务**前必读:§2 方法论(值得保留)、§3 坑清单(必避)、§4 资产索引、§6 未决项。
> **关联**:覆盖 `docs/HANDOFF_20260904.md`(其 §2 环境/§5 训练协议仍有效);本文件聚焦本窗口新增。
> 交接时状态:`main == origin/main == 1f1c577`,工作树干净。

---

## 1. 一句话现状(接手即知)

四支柱证据链已闭合到"支柱③判定层":合成侧情景丙(exp12v4)、真实侧情景乙(exp8R v3.1:2/10 显著正、5/10 显著负、ball 假性负号消除)、GBR 破缺归因改写(exp9d:终止子通道排除)。**主智能体裁决冻结中**(选项 C 分层条件化升首选),专家第六轮汇报延后至裁决。云依赖已解除(本机速度代际 ~1 min/物体级)。**OPT-1 VarPro 阶段 1 全绿入库但阶段 2(五判据)未跑**——新计划若继续该线,从 §6.1 接。

---

## 2. 值得保留的方法(按价值排序,每条含证据指针)

### 2.1 数值实验纪律层(最高价值,直接复用)

| 方法 | 一句话 | 证据/出处 |
|---|---|---|
| **预注册三分支** | 判据+分支(甲/乙/丙)跑前写死,任何分支不停手;禁事后改判据 | `exp8r_preregistration_v3.md` 全系列 |
| **已知答案单测先于跑批(红线#8)** | 任何 Schur/边缘化/推前实现,入库前过随机小规模稠密 pinv 对照(rel err<1e-10);本窗口两次实证救命 | `exp8r_diligent_discrimination_v31.py::known_answer_test`(4.6e-16);`exp8r_varpro_estimator.py` 三项 |
| **跨机逐子集对账** | 固定种子下 σ_min(子集几何)对齐 ≤1e-16 = 数据链逐子集一致的自检;LAE 差<0.01° = 估计器等价 | exp8S σ 对齐 1e-16;v3.1 对账 0.0002° |
| **审计结论范围纪律(红线#12)** | 审计结论只覆盖其实际检验的变体,禁跨对象外推("三假说排除"教训) | v3.3 §4.3;`exp8r_verdict_v3.md` §3 修订段 |
| **oracle 对照臂** | 真值几何/参数下重算诊断,与拟合值版差 = "拟合点污染"量化 | `exp8s_oracle_schur_audit.py` 三变体 |
| **异质性先于合并(红线#3)** | 判据在物体内算(n=50),物体间只计数;方向一致性不过 → Fisher 合并禁用 | v3.1 verdict §1 |
| **调节变量升主表旁列** | 非朗伯程度/离群占比与相关符号的分层,失败也是发现(pot1 反例) | v3.1 verdict §3 |
| **多起点纪律(红线#1/#11)** | GT 只准进评分;初始化数据驱动 + `assert not np.allclose(z_init, z_true)` | exp12v4 D1;烟测 MAE 0.75° 无 GT |
| **降档决策基于实际运行路径(红线#10)** | 算法替换后旧耗时作废,降档前用新路径实测重估 | exp12v3 "35min→0.7s" 教训 |

### 2.2 数值实现层

| 方法 | 一句话 | 证据 |
|---|---|---|
| **解析+稀疏 Jacobian + LSMR** | scipy trf 对稀疏 J 自动走 LSMR,正规方程 O(m·n²)→O(nnz);**紧公差 tr_options={maxiter:500, atol:1e-12, btol:1e-12} 保同最优解** | 5e8c332;FD 对照 cost 差 4e-11 |
| **FD 一致性闸** | 解析 J 逐列 vs 中心差分 rel err<1e-5 才可信(4e-9 实测) | `exp8r_jac_validation.py` |
| **全暗/退化像素过滤** | F_rr=0 或 ρ_e=0 的像素 0/0→NaN→eig/pinv 崩;过滤 = keep0>1e-3·median 口径(exp11v3 同源) | exp12v4 hemisphere 崩修;v3.1 SVD 崩修 |
| **跨进程确定性种子** | `zlib.crc32(name)` 替代 `hash()`(PYTHONHASHSEED 污染);抽样全预生成使断点续跑=一口气跑完 | 825e71f |
| **子集级断点续跑** | 每 5 子集落盘 partial(ci_done/oth_done 游标),重跑自动跳过;判据零改动 | 009dffe |
| **VarPro 可分离加速(阶段1已验)** | 双闭式 ALS(ρ 逐像素/α 逐光)+ 外层 2N 维 GN;**外层 J 必须含链式项(θ±h 重 ALS 后差分)** | `exp8r_varpro_estimator.py`;1.2e-7 cost 差/1.38× |
| **工作带谱截断** | tr(F⁻¹) 类量在 λ<1e-3·λ_max 丢弃(1e-6 边形同虚设:2048/2048 入带);负特征值不入带 | exp12v4 D3 定标准录 |

### 2.3 工程/云运维层

| 方法 | 一句话 | 出处 |
|---|---|---|
| **一键入口模式** | 环境自检→venv→数据探测(静态+find 递归兜底)→断点续跑→并行池→合并→打包 zip,失败重跑同命令即续 | `run_exp8r_cloud.sh` 全家桶 |
| **BLAS 线程钉扎** | N 进程并行时每进程 OMP/OPENBLAS=核数/N,防 N×核 超额订阅(曾 267× 浪费) | 825e71f |
| **会话无关启动** | `nohup + disown` 包装(SSH 掉线曾三次杀启动器丢合并步骤) | `run_exp8r_cloud_daemon.sh` |
| **进度可观测** | 每子集 `[P] done=x/110 el eta` 行 + `show_progress.sh` 总览表(DONE/RUN/PENDING+ETA) | 825e71f |
| **git 代理端口轮换** | 系统代理频繁变(58240→…→**63770 当前**);push 失败先查注册表 ProxyServer 再换端口 | 本窗口 5 次轮换 |
| **两副本同步纪律** | cloud_migration/ 与 critical_experiments/ 双副本必须同步提交(曾漏 cloud 副本致云跑旧代码) | 1141271 教训 |
| **子代理分工** | 独立可验证任务(单测+固定数据)交后台子代理,主线程做不冲突卡;纪律写死在指令里(不放宽阈值/停手即报) | OPT-1 阶段 1 全绿 |

---

## 3. 必避之坑(每条含一次真实事故)

### 3.1 数值/算法坑

| # | 坑 | 事故 | 规避 |
|---|---|---|---|
| 1 | **`J[slice, arange]` 花式索引外积** | 解析 Jacobian ρ 块整体错填,优化器收到垃圾梯度(cost 24202 vs 185) | 逐像素对角块用 `np.diag` 显式构造;FD 闸必过(正是它拦截的) |
| 2 | **死代码活到生产** | `als_init` 三行开发遗留 `M[:,k]`(N=3 不炸,N=5 炸) — 几周未暴露 | 删死代码不留注释悬念;**加速后把所有 N 路径快速烟测一遍**(1s/子集让 30s 全扫成可能) |
| 3 | **docstring 声称≠实现** | "投影掉强度维"注释在,代码无 α Schur 项(v3.3 判 exp8R 两代数错误之一) | 检查表第 5 行:声称被边缘化的每个参数必须有对应代码项,以代码为准 |
| 4 | **GT 初始化幻象** | `z_als = z_true.copy()` 注释写"常数场";LM 7 迭代"收敛" 0.44° | assert + 数据驱动多起点;GT 只进评分 |
| 5 | **ρ 边缘化口径错位** | 全局单标量 `(Σx)²/(Σd)` ≠ 逐像素对角 `Σx²/d`(F_ρρ 本是对角阵) | 写 Schur 前先问"F_ρρ 到底什么形状";单测对照稠密 pinv |
| 6 | **档位低于统计可行性** | configs=6 时 n=6 临界|ρ|≥0.886,"≥3/4 显著"数学不可达 | 降档前算临界 ρ;档位与判据联动检查 |
| 7 | **近规范方向污染迹** | tr 跨 42.6→6.6e15(12 个量级),巨大值=数值噪声秩 | 工作带截断 + sanity 范围闸 [1e-3,1e8](度²) |
| 8 | **散点归因的桶定义 bug** | `\|Zn\|<0.05` 归一化"或"语义 → 87% 覆盖,桶无意义 | 桶定义先画覆盖率;逐光窄带(43%)+ 距离分桶 |
| 9 | **VarPro gauge 平坦** | (ρ,α)→(cρ,α/c) 不变 → 落点规范不同,raw 对照假性 FAIL | 对照用规范不变量(方向/cost/LAE/ρα 乘积矩阵) |
| 10 | **VarPro 外层 J 缺链式项** | 固定 (ρ,α) 差分 → GN 线性收敛 1.7e-4 rad | θ±h 处重新 ALS 收敛后再差分 |

### 3.2 工程/流程坑

| # | 坑 | 事故 | 规避 |
|---|---|---|---|
| 11 | **ssh 会话杀前台** | "Terminated" 三次(掉线/平台收杀),合并步骤丢失 | daemon 包装(nohup+disown)或 tmux |
| 12 | **云端 fetch 时间差** | 用户 fetch 到旧 commit,以为新脚本无效 | 提交后 ls-remote 验证;给用户"确认 HEAD=xxx"步骤 |
| 13 | **占位/哑代码进矩阵** | `g_a/rho*rho` 占位算式 rho=0 时 NaN→SVD 崩(10/10 物体) | 占位立即清;任何算式过单测再进主管线 |
| 14 | **字符串锚点替换反斜杠歧义** | python heredoc 里 `\\`+续行锚点多次匹配失败,曾半写入 | 用字面量拼接构造锚点;替换后 ast.parse/bash -n 立即验 |
| 15 | **未验证 URL 入库** | 曾写两个猜测下载直链(违反数字卫生),主动移除 | 禁硬编码未验证 URL;给三条可靠路径(本地上传/官方页/显式变量) |
| 16 | **数据不在仓库假设错位** | "clone 即跑"不成立(936MB DiLiGenT 被 gitignore) | 入口脚本内建数据探测+三路径指引 |
| 17 | **多进程 BLAS 超额订阅** | 10×64 线程抢核,CPU 无界爬满,曾 267× 浪费 | 按进程数钉扎线程(核/N) |
| 18 | **依赖系统级 python** | 云上 pip 装全局可能失败/污染 | venv 自动创建;.venv 存在则沿用 |

### 3.3 统计/科研叙事坑

| # | 坑 | 事故 | 规避 |
|---|---|---|---|
| 19 | **Fisher 法误用** | 异质效应合并 Fisher's method 得假阳性(meta-p 污染) | 先方向一致性;不过则只计数 |
| 20 | **负结果外推过度** | "三假说全部排除→非实现缺陷"超出审计覆盖 | 结论句逐对象对应 |
| 21 | **先验强度影响判读** | exp12v4 敏感性 λ×10 方向翻转(ρ −0.19) | 敏感性档位并列报告 |
| 22 | **打印精度掩盖真值** | trace ~7e-12 被 `%.6f` 显示成 0,误判"恒零" | 全精度 json 落盘 + 秩相关(量纲无关) |

---

## 4. 资产索引(新任务可直接引用)

### 4.1 判定与结论(当前唯一有效读数)

| 对象 | 读数 | 文件 |
|---|---|---|
| 合成侧判别(支柱③) | **情景丙**:0/4 显著正,3/4 方向正(sphere+0.336/cube+0.067/hemisphere+0.318),cylinder −0.697 显著负 | `exp12v4_discrimination_matched.{py,json}` |
| 真实侧判别(支柱③) | **情景乙**:2/10 显著正(ball +0.303/pot2 +0.474),5/10 显著负(pot1 −0.783/buddha/cow/harvest/reading),方向 3正7负 | `exp8r_verdict_v31.md` + `exp8r_v31_summary_stats.json` |
| GBR 破缺归因 | SH 截断 <4%(SH-4 实测);λ 主源=轮廓带(exp9c-1 降 45% + exp9d 富集 3.4×);终止子通道排除(<5×);η 扫描单调=ReLU 全局激活度 | `exp9c/exp9d_*.json` |
| exp8R v3 污染判定 | **禁引** headline"0/10 正 6/10 负"(两代数错误),已标 SUPERSEDED | `exp8r_verdict_v3.md` 头部 |
| exp12v3 | **VOID**(四缺陷),保留 divergence_precheck(残差比 1e11-1e16×40 永久有效) | v3.3 §1.1 |
| VarPro 估计器 | 阶段 1 全绿(单测 2.6e-16/1.7e-16/1.5e-8 rad;烟测 1.2e-7/1.38×);**阶段 2 五判据未跑** | `exp8r_varpro_estimator.py` + `exp_opt1_microbenchmark.{py,json}` |

### 4.2 可复用基础设施

- **估计器链(验证过)**:`exp8r_diligent_discrimination_v3.py` 的 joint_trf/trf(解析稀疏 J)/als_init/calibrate — FD 4e-9、同最优解 4e-11、跨机对账 0.0002°;
- **诊断量正确代数模板**:`diagnose_trace_v31`(变体 A 移植,单测 4.6e-16);
- **云全家桶**:`run_exp8r_cloud.sh`(根引导)+ `cloud_migration/`(入口/merge/进度/daemon);
- **骨架与措辞**:`docs/paper/ICCV2027_SKELETON_v1.md`(支柱③行已重标 ⏸ 待 R2/S2——**注意:R2/S2 现已完成,新 agent 需把这些行更新回终态**)、`docs/论文措辞包_v0.4.md`(8+1 条)、`docs/论文数字口径说明_v0.5.md`(禁引清单);
- **红线体系**:v3.0–v3.3 共 12 条(#8 已知答案单测/#9 边缘化声称=实现/#10 降档实测/#11 GT 显式声明/#12 审计范围)。

### 4.3 环境(继承 09-04 版,新增项加粗)

| 项 | 值 |
|---|---|
| 仓库 | `D:\MIR_Archive_20260829\Multi-Illumination-Inverse-Rendering\`(checkpoint 在父目录,不在 git) |
| **git push 代理** | **127.0.0.1:63770(当前,会轮换 — 失败先查注册表 ProxyServer)** |
| 数据 | `D:\data\DiLiGenT\pmsData`(936MB)、`D:\data\synthetic_v3` |
| 本机 | 32 核/16GB 内存(物理紧张,大 P 需子采样 ≤2000)/RTX 5070Ti 12GB |
| **云经验** | 64 核实例对稀疏 J 任务无额外收益(单进程秒级子集);云仅剩"冷备份"价值 |

---

## 5. 本窗口 Git 时间线(溯源用)

```
1f1c577 卡S2 v3.1 全量收口(情景乙) + OPT-1 阶段1入库
c804fd0 v3.1 诊断代数修正(变体A移植)
f77e779 卡X exp9d 终止子排除(判别c重写)
f707786 卡R2 exp12v4 全量(情景丙)
8d5e39d 卡R2 代码 + 卡W2 骨架重标/口径v0.5
58b3397 卡S收口(后被判污染) + 卡W + 卡I
3411c38/f0c62e9 exp8S oracle 审计(变体A=正确实现,成为v3.1移植源)
2253fdb-72c2c59 云全家桶六连(daemon/断点/钉扎/进度/路径)
5e8c332 解析+稀疏 Jacobian(性能代际, 267×)
009dffe/825e71f 断点续跑/进度显示
46e7a9f als_init 死代码修复(N>3 崩)
1037da7/506afd7 exp12v3 LM 化(后被判 VOID)
```

---

## 6. 未决项(新计划开局清单)

### 6.1 直接可续(如果新计划仍含此线)

1. **OPT-1 阶段 2(五判据)**:ball+harvest × 25 子集(σ_min 分层 20+大 N 5),判据表在 `C:\Users\35702\Desktop\新.md` §2 阶段 2;**gauge 风险已列**(对照用规范不变量),N=50 成本风险未验;全过→阶段 3 本机全量替换主管线;
2. **骨架更新**:R2/S2 已完成,`ICCV2027_SKELETON_v1.md` 的 ⏸ 行需翻回终态 + 措辞包条目 5 重写(v3.1 数字);
3. **支柱③终局裁决**:选项 C(分层条件化)为主智能体保留裁决——合成情景丙+真实 pot1 反例+压力轨三线证据;分层变量候选=几何复杂度(需新预注册);
4. **专家第六轮汇报**:素材全齐(R2 情景丙/S2 情景乙+调节变量/X 归因重写),纯文字;
5. **卡H seed2024**:唯一 GPU 臂(~8h),`bash run_safe_arms.sh --only A3-2_seed2024 ...` 待排;
6. **harvest 10 个 N=50 子集存档缺口**(云实例暂停遗留,不影响任何判据,可选补)。

### 6.2 新计划建议优先考虑

- **去云化已事实达成**(1 min/物体),新任务的数值实验默认本机;云只在"单子集 >10min 且可进程级并行"时才值得;
- 新实验若涉及 Fisher/Schur/迹推前:**直接 import `exp8r_diligent_discrimination_v31` 的单测模式起步**(known_answer_test 是模板);
- 大 P(>2000)任务:先跑内存估算(exp12v2 曾 36.9GB 崩),子采样+固定种子+预注册声明。

---

## 7. 三条铁律(浓缩版,新 agent 每日自问)

1. **跑前**:判据写死了吗?单测过了吗?GT 只在评分吗?
2. **跑中**:进度可见吗?断点可续吗?线程钉了吗?
3. **跑后**:结论和检验对象一一对应吗?禁引清单(`口径说明 v0.5`)核对了吗?两副本同步了吗?push 用当前代理端口了吗?

---

*交接执行 agent · 2026-09-07 · 本窗口:任务书 v3.2 全卡 + v3.3 修复轮(R2/W2/X/S2)+ OPT-1 阶段 0-1 · HEAD 1f1c577*
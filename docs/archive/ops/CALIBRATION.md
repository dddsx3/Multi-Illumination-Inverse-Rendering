# CALIBRATION · 夜跑时长标定表

> 归属：任务书 T v2.0 条例 T0-1 · 目的：标定单臂时长，供 OP-3 每日节奏与 T0-4 情景裁定使用。
> 平台：本机 RTX 5070 Ti Laptop（12GB）· 热墙 _env.sh 固化 75/80/88。
> 标定对象：A3-0 F-N5 复现臂（fusion + gray + N5 + bf16 @ **bs4（降配，INC-0014）**，256²，v3 划分）。

| 日期 | 室温 | 场景 | 单臂时长(实测) |
|---|---|---|---|
| 2026-09-03 | 室内空调环境（无温度计，估 24±2°C） | run_arms 实测标定（bs4, nw2）：bench proxy 0.4658 s/batch × 112 batch × 开销系数 3.38 | **370.6 s/epoch ≈ 9.8–10.3 h / 100 epoch** |
| 2026-09-03 | 室内 | 温度墙循环：撞墙 rc42 → 存档 → 冷却 ≤75°C → 自动续跑（74–79°C 波动，实测多轮闭环正常） | 常态热墙开销 |
| 2026-09-04 | 室内（夜间 00:32–02:39 + 白天 04:05–10:48） | A3-1 实测（logs/A3-1_noFiLM tfevents 时间戳重建）：夜间 00:32 起跑后两段长停（4895s/7928s，~00:32–02:39，疑似 INC-0016 加载窗口熔断盲区/非热墙中止，epoch 0–17 段）；04:05 起 run_arms 续跑段 epoch 17→100 连续无 rc42 | **实测 287.8–292 s/epoch（基准锚点，GPU 连续占用口径）**；有效训练（不含停机）≈ 100×292s ≈ 8.1h；墙钟跨度 00:32→10:48 ≈ 10.3h |

## 标定数字（T0-1 验收输出）

- [x] **断点恢复后 epoch 连续无跳变**：冷重启断点 epoch 5（checkpoint_epoch_0004.pth）→ 续跑日志 `[segment] epoch 5 -> 15`、命令 `--resume checkpoint_epoch_0004.pth`，续跑起点 = 中断点 ✓
- [x] **实测 s/epoch 写入**：370.6 s/epoch（来源：_arm_A3-0_run.log / arms_plan.json，bench proxy 口径）；**09-04 A3-1 实测修正：287.8–292 s/epoch（直接 epoch 间隔口径，见首表新行）**——370.6 为 bench proxy 保守估计，实测显著更快，排期按 8.1–8.5h/100ep 计
- [x] **温度墙行为记录**：rc=42 优雅存档 + 冷却自动续跑闭环通过；INC-0014 后主机内存熔断复用同一通道
- [ ] 室温补测后回填首行

*R-D 双锚点结论（FIX-08-2）：夜间（00:32–02:39）样本被长停打断、无有效连续 epoch 间隔；白天各小时（04–10 时）epoch 中位间隔 291–295s，无可见"夜间更快"证据。**不增补独立夜间锚点行，以 09-04 全窗实测 292 s/epoch 为新基准锚点**（不区分昼夜，370.6s 保守值保留历史注记）。*

*2026-09-03 回填。注：标定对象为 bs4 降配口径；历史 bs8 基线的本机不可行性见 INC-0014。*

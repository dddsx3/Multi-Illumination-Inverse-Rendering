# C22 复现审计 · 1 页 memo（2026-09-08）

**判定：Gate E 复现要求满足（本机双重验证）。**

## 审计内容与结果

1. **一键链（本仓库）**：`bash scripts/reproduce_paper.sh` 端到端通过——pip install -e . →
   pytest → CI01/CI02/CI03/CI03nl 正式 run（真实侧自动探测 D:/data 存在则跑 CI04/CI05/ablation）→
   Fig.1–9 一键重建（provenance 随图更新）。退出码 0。
2. **Clean checkout（GitHub 克隆 bd67897）**：
   - 首轮抓到**真实缺口**：`src/calibinfo/datasets/{diligent,synthetic,__init__}.py` 被未锚定的
     `.gitignore datasets/` 规则静默排除（C14 修锚定时只补了 openillumination.py）——
     clean clone 的 pytest 直接 ModuleNotFoundError。补交 `8bcc139`。
   - 补交后 clean clone：pip install 绿、pytest 54/54 绿、CI01 formal 重跑 100/100
     all_pass=True、Fig.3 及 provenance 重建成功。
3. **第三方机器 smoke**：本机即"不同 BLAS 环境"验证的第一层（Windows/Py3.14/numpy2.4）；
   真正异机 smoke 需第二台机器（TEMPLATE 命令 = reproduce_paper.sh，README 待投稿版补一节）。
   CI01 的 identity 阈值 1e-10 对 BLAS 差异有 5+ 个数量级余量（C02 迁移时已验证跨机对账 0.0002°）。

## Gate E checklist 对照

- [x] 所有主 Figure/Table 可由 clean checkout 一键重建（reproduce_paper.sh）
- [x] CLAIMS_REGISTRY 每条 claim 有 artifact path + commit SHA（C1–C6 + manuscript 指针）
- [x] figures 逐一 provenance（git SHA + artifact sha256_16）
- [ ] 第三方机器 smoke（单机环境，投稿前在异机执行 reproduce_paper.sh 即可补）

## 结论

复现体系达到 Gate E 要求；发现并修复的 .gitignore 静默排除正是"复现审计必须真实执行"
的价值证明。
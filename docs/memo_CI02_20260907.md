# CI02 · 1 页 memo（2026-09-07 · 卡 C10）

**判定：CI02 通过（两项预注册 gate 全绿）。** 54 分层场景（3 几何 × 3 albedo × 3 灯仰角 × {1,3} 灯，
P=300，工厂分层网格），config=`configs/ci02/ci02_formal.yaml`，
摘要=`artifacts/frozen/ci02_formal_summary.json`（manifest 同放）。

## 核心数字（预注册 gate）

| 指标 | 观测 | gate |
|---|---|---|
| 闭式（Prop 2 多灯求和）vs 直接 Rayleigh（良条件区） | p50 3.1e-09，p90 8.6e-09 | <1e-8 ✅ |
| 同上（含消减地板分层预算，全程 54×25 网格） | layered max **0.132**（<1 即过） | ✅ |
| λ⋆ 预测误差 log₁₀(pred/scan)，闭式单调曲线二分求根 vs 一阶 μ_floor/Σαᵢ² | **median 0.00000，q90 0.00003**，max 8.1e-05 | H3 ≤1 decade ✅ |
| λ⋆ 适用条件 λ⋆ ≪ min_{αᵢ≠0} sᵢ² | cond max 0.99，p50 0.026（54/54 在线性域） | 诊断项 ✅ |
| retention 固定网格（within-scene） | bounds 全 ok（C11 加双路线+追踪） | ✅ |

## 实现层两处处置（如实记录）

1. **灾难性消减地板**（首轮 3/54 场景 cvd 1.1–2.6e-08 触发 1e-8 gate）：深未校准端
   gauge Rayleigh ≈ λΣαᵢ²→0，直接路线相对误差地板 ≈ C·eps·saturation/|direct|
   （与红队 V2 同现象：其 25-decade max 3.9e-8）。处置=阈值分层（宪法 §6.2）：
   gate 语义改为"良条件区 <1e-8 且全程 < max(1e-8, 100·eps·sat/|direct|)"，
   cancel_margin=100 预注册入 config；数值全部保留非改判。
2. **λ⋆ 读出升级为二分求根**：首轮网格分辨率把 median|log10| 限在 0.36（网格 1 decade
   步进所致）；按宪法 §4.2"否则使用闭式数值求根"改为闭式单调曲线二分（80 步，
   相对收敛 1e-12）。升级后 median=0.00000——**一阶预测器 μ_floor/Σαᵢ² 在线性域内
   与精确求根数值不可分**，比红队 V3 单场景 0.74（网格分辨率限制）强两个量级以上。

## 有效性结构（H3 有可解释结构，非空洞通过）

- log10 误差全部 ≤0（预测 ≤ 扫描值，54/54）：一阶近似在交叉点略保守——
  二阶项 λ²Σαᵢ²sᵢ⁴... 使闭式曲线在交叉前已抬升；
- 误差与适用条件相关：cond=λ⋆/min sᵢ²(αᵢ≠0) 最大的 8 个场景（>0.1，全部 sphere/wide
  类平坦谱）贡献全部 ≥1e-5 量级误差；cond<0.1 的 46 场景误差 ≤3e-05；
- 无任何场景落入"λ⋆ 与 sᵢ² 同量级"的失效带（max 0.99 仍 <1）。

## 结论与下一步

- C3（gauge 精确谱闭式）多场景确认；C4（λ⋆ 场景量预测）在全部 54 场景线性域内
  median 0.00000——远超 H3 的 1 decade 判据；
- Fig.3 代表场景已存 frozen（闭式曲线+floor+λ⋆ 双标记+固定网格 retention）；
- 下一步 **C11**：retention generalized-eig/白化双路线交叉（<1e-10）+ track_modes
  连续性追踪生成 tracked ρ heatmap（禁索引排序）+ V6 回归 → Gate(10-10) 判定进 CI03。

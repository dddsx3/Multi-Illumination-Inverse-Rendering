# CI03 非线性 validity envelope · 1 页 memo（2026-09-08 · 卡 C13）

**判定：Gate C 通过——有效域存在，真实预测主张限定在低 mask-flip 区。**
B 臂（解析非线性 SH+ReLU，同 GT 重渲染）42 cells（3 几何 × 2 场景 × 7 强度档），
config=`configs/ci03/ci03nl_formal.yaml`，摘要=`artifacts/frozen/ci03nl_nl_formal_summary.json`，
Fig.6 draft=`paper/figures/fig6_draft.png`。

## 核心数字（预注册 gate_valid_pred_err=0.10）

| 几何 | <10% 有效域边界（mask-flip 率） | 最大有效 k（×Σ_c0） | 有效 cells |
|---|---|---|---|
| sphere | flip ≤ 3.00% | k ≤ 0.50 | 5/14 |
| bumpy | flip ≤ 0.80% | k ≤ 0.25 | 4/14 |
| terminator_heavy | flip ≤ 2.15% | k ≤ 0.50 | 5/14 |

误差-机制单调性（R-B 风险坐实）：三几何的弱模式 median 误差随 flip 率单调爬升
（flip ~0.3% → err 0.6–2.8%；flip ~20% → err ~60%），且 <10% 域全部落在 flip ≤3%——
**线性化失效由 terminator 翻转驱动**，与宪法 R-B 的预测一致；mask-flip 全程逐 cell 记录
（验收红线：缺失=不合格，已满足）。SNR 轴已记录（11.7–12.8 @σ=0.02）供 Fig.6 扩展面板。

## 协议要点（写进论文 Section VI）

- 估计器 = **线性理论估计器**（x̂=ΔF⁻¹AᵀMy @ nominal 线性化）——与理论匹配，
  隔离"模型失配"效应（非线性重估计会把估计器能力与理论有效性混在一起）；
- 真渲染 = relu(Y(c₀+δc)) 逐 draw 重算掩码（flip = |H(δc)≠H₀|/P）；
- 增强臂 A（BlenderProc 真渲染）按卡条款为增强项：B 臂交付即主线闭合；
  A 臂待 GPU 排期（与遗留卡H 同队列），非阻塞。

## Gate C 处置

- 有效域存在 → **不做**"只留线性理论"的降级；CI04 真实数据的 corruption 设计
  必须把注入量控制在低 flip 区（或显式报告 flip 率并在 envelope 外标注失效）。
- 下一步：**C14 OpenIllumination 获取与适配**（HF 列文件实测单对象体积 → 降采样
  裁决入 manifest → development 6–10 对象清单冻结）。

# 强制修复书 v1.0 · 开工前必做清单（2026-09-04）
> 依据：审计裁决书_20260903（世代更替 + 口径修正）· 性质：**门禁前置**——本册全部条例完成并逐条验收后，打标签 `gate-FIX-20260904`，此后才允许启动任何新训练（EX-03 A3-1）。
> 总原则：本册全是文档/配置修复与防再犯机制，**零新增 GPU 训练**（仅 FIX-06 需一次冒烟级验证 ~20min）。所有数字修正一律以入库 eval json 为源（R6），禁止凭记忆改数。
> 与任务书关系：《任务书T_执行条例版v2.1修订附录》是条例层修订（本册 FIX-07 负责落地），两册配套。

---

## 0. 错误/问题总账（先看这一节，每条对应一条 FIX 条例）

| # | 问题 | 严重级 | 证据 | 处置条例 |
|---|---|---|---|---|
| P1 | README §4.1 主结果表 **albedo 0.0532 跨臂混标**（真值 gray 0.1279/rgb 0.1304；0.0533 属 resA/albOff n_curve） | 🔴 论文级（审稿人查表即爆） | G0 清点表 §一 | FIX-01 |
| P2 | README 主结果表**世代混排**：历史 bs8（pre-constraint、ckpt 缺失）与 Gen-A3 数字同格 | 🔴 论文级 | 裁决书 E1/E2 | FIX-01 |
| P3 | N 曲线叙事（N_min=1 / 极差 0.030°）**出自非主臂**（resA/albOff 世代 n_curve ≈10.35° vs 主臂 7.79°），协议与 test 不可比 | 🔴 论文级 | 裁决书 E8 | FIX-01（注记）+ EX-01（重测） |
| P4 | CLAIM_CARDS S-01 仍记历史世代为主交付，A3-0 数字未回填 | 🟠 | CLAIM_CARDS.md | FIX-02 |
| P5 | CLAIM_REGISTRY 版本头 v0.6 vs 正文 v0.7 不一致 | 🟡 | CLAIM_CARDS 头注记 | FIX-03 |
| P6 | G0 资产清点表缺"可否进论文"最终列（A3-0 eval 已出，可定稿） | 🟠 | G0 清点表 | FIX-04 |
| P7 | RUN_CARD 无世代指纹 → 世代漂移无防再犯机制（10h/臂的学费） | 🟠 | 裁决书 E10 | FIX-05 |
| P8 | run_arms.py BATCH_SIZE 默认 8（本机必崩值），裸跑入口是陷阱 | 🟡 | INC-0014 | FIX-06 |
| P9 | 任务书 T v2.0 判据包 4 引用已除名数字 0.0532（≤0.064） | 🟠 | 裁决书 §3 | FIX-07 |

**完成定义**：P1–P9 全部转绿（验收行打 ✓）+ 标签 gate-FIX-20260904 → 进入"下一步册"（EX-01 → EX-02 → EX-03）。

---

## 1. FIX 条例卡

### 条例 FIX-01 · README §4.1 重构（P1+P2+P3）
- 目标：主结果表世代双行、0.0532 除名、N 曲线注记。
- 输入：`README.md` §4.1；`eval_output/A3-0_f_n5gray_seed42_test/eval_summary.json`（Gen-A3 行数字源）；`eval_output/p2_t22_f_n5gray_test/eval_summary.json` + `p2_t22_f_n5rgb_v2_test/eval_summary.json`（reference-only 行）。
- 步骤：
  1. 打开 README §4.1 当前主结果表，先通读全文找出**所有**引用 0.0532 / N_min=1 / 极差 0.030 的句子（不止表格）；
  2. 表格重构为两行世代（示例结构，数字以 json 为准，std 一并写入）：
     - **Gen-A3（当前协议 · bs4 · 物理约束头）**：normal MAE 10.30°±3.75｜PSNR 32.55±3.50｜albedo si-MAE 0.1482±0.045｜depth RMSE-aligned 0.338｜物理违规 0.0000%（来源：A3-0_f_n5gray_seed42_test）
     - **历史世代（reference-only · pre-constraint · ckpt 永久缺失，不可复现）**：F-N5 gray 7.792°/36.09/0.1279；F-N5 rgb(v2) 8.177°/37.25/0.1304——注明"含已修复缺陷（输出无约束，曾出现负 albedo），仅供消融叙事参考，禁作对比基准"；
  3. albedo 0.0532 全文除名：Ctrl-F 逐处处理（删改/标注来源为 albOff n_curve），最终 0 命中；
  4. N 曲线段加注记："极差 0.030° 出自旧消融世代（resA/albOff）n_curve 子采样协议，与主臂协议不可比；Gen-A3 重测进行中（EX-01），N_min 声明待重测结果定稿"；
  5. 若 README 存在 normal MAE "~35°" 类注记 → 与 `p2_r0_v3gray_test` json（10.66°）核对后修正或删除。
- 产物：README.md 修改。
- 证据：README diff（git）+ Ctrl-F 截图或计数行记录（0.0532 命中数 = 0）。
- Git 提交：`fix(doc): README §4.1 世代双行重构 + 0.0532 除名 + N曲线注记 [FIX-01]`
- 验收：✓ README 无 0.0532（除引用注记注明出处外）；✓ 世代分行无同格混比；✓ N 曲线注记在；✓ R0 行数字与 json 一致。
- 卡住时：README 结构与描述不符 → 原样贴给主智能体，不自行发挥。

### 条例 FIX-02 · CLAIM_CARDS S-01/S-02/S-04 世代修订（P4）
- 目标：叙事卡与 Gen-A3 世代对齐。
- 步骤：
  1. S-01 卡：主数值行改 A3-0（10.3039° / 32.546 / 0.1482，std 附注；来源文件改 `eval_output/A3-0_f_n5gray_seed42_test/eval_summary.json`）；历史 7.792 移入卡内"reference-only 对照"小节；口径栏追加：允许——"Gen-A3 世代（bs4+物理约束）"标注；禁止——无世代标注直接引用历史数字；
  2. S-02 卡：加世代注记（0.030° 待 EX-01 重测后定稿，重测前禁止作为 N_min=1 的引用源）；
  3. S-04 卡：加注"数值出自历史世代 ckpt，EX-02 重测后回填"；
  4. 禁词自检段照跑一遍（抄 CLAIM_REGISTRY 禁词清单）。
- 产物：CLAIM_CARDS.md 修订。
- Git 提交：`fix(doc): CLAIM_CARDS S-01/S-02/S-04 世代对齐 [FIX-02]`
- 验收：✓ 三卡世代注记齐全；✓ 禁词自检无命中。
- 卡住时：同上。

### 条例 FIX-03 · CLAIM_REGISTRY 版本头统一（P5）
- 步骤：文件头 `v0.6` → `v0.7`，并与正文 v0.7 裁决段核对一致；同步检查 git 标签/提交信息中版本引用。
- Git 提交：`fix(doc): CLAIM_REGISTRY 版本头 v0.6→v0.7 统一 [FIX-03]`
- 验收：✓ 版本头=正文=提交信息。
- 卡住时：正文出现 v0.7 之外的版本残留 → 逐处列出贴给主智能体。

### 条例 FIX-04 · G0 资产清点表定稿（P6）
- 步骤：补"可否进论文"最终列——A3-0 世代行标"可进（S-01 终稿数字）"；历史行标"reference-only"；缺口表逐行复核（已闭合的标 ✓，未闭合的标归属条例）。室温补测回填 CALIBRATION.md 首行（温度计或无 → 写"室温≈室内空调环境"）。
- Git 提交：`docs(g0): 资产清点表定稿 + CALIBRATION 室温回填 [FIX-04]`
- 验收：✓ 清点表所有"待核/待定"项有终稿或映射条例。
- 卡住时：ckpt 无法定位的条目按 INC-0014 注记处理，不删条目只标注。

### 条例 FIX-05 · RUN_CARD 三指纹落地（P7）
- 目标：世代漂移防再犯机制——每个 run 可追溯到代码/config/数据。
- 步骤：
  1. 编辑 `docs/run_card_howto.md`：追加三指纹生成命令——`code_commit_sha`：训练启动前 `git rev-parse HEAD`（Windows: `git rev-parse HEAD` 同）；`config_hash`：训练 config 全量 json（`arms_plan.json` 对应臂段或 run_arms 存档）的 SHA256（PowerShell `Get-FileHash` / certutil）；`data_manifest_sha`：`splits/synthetic_v3.json` 的 SHA256 + 数据目录文件数/总大小；
  2. RUN_CARD.json 模板追加三字段（示例值格式见 howto）；
  3. A3-0 补记：`git log` 追 A3-0 训练启动时刻（账本：09-03 13:34）的 HEAD sha；config 取 run_arms 存档的 arms_plan；数据 hash 现算现记。**若启动时刻 HEAD 无法确证 → 标注 `"code_commit_sha": "unrecorded (13:34 启动前后 HEAD 区间 e24cd5d~先, 见 git reflog 不可得)"`，诚实注记不伪造**；
  4. 把"世代变更行"登记格式写进 howto（Gen-A3 → Gen-A4：任何代码改动后首训前，账本登记变更理由 + 相关 commit）。
- 产物：run_card_howto.md 修订 + A3-0 RUN_CARD 回填。
- Git 提交：`fix(doc): RUN_CARD 三指纹协议 + A3-0 回填 [FIX-05]`
- 验收：✓ howto 含三命令可直接照抄；✓ A3-0 RUN_CARD 三字段在（或显式 unrecorded）。
- 卡住时：hash 命令在 PowerShell 报错 → 把报错贴主智能体（certutil 语法变体问题，不自行绕路）。

### 条例 FIX-06 · run_arms.py 默认 batch 修正（P8）
- 目标：消除"裸跑即崩"陷阱入口。
- 步骤：① `run_arms.py` 中 `BATCH_SIZE` 默认 8 → **4**（保留 `RUN_ARMS_BATCH` 环境变量覆盖通道，注释注明"bs8 需 ≥16GB 显存机器，本机 INC-0014 实测不可行"）；② `run_safe_arms.sh` 降配提示文案同步（bs4 默认后提示只在不满足时出现）；③ 冒烟验证：`python run_safe_arms.sh --smoke`（或脚本内自检）确认 bs4 路径 PASS、bs8 路径 BLOCK 提示正确；④ main.py 的 `--batch_size` 默认已是 4（勿改）。
- 产物：run_arms.py + run_safe_arms.sh 修改。
- Git 提交：`fix(run_arms): 默认 batch 8→4（本机 12GB 实测不可行，INC-0014）[FIX-06]`
- 验收：✓ 冒烟自检 bs4 PASS / bs8 BLOCK 文案正确。
- 卡住时：冒烟报错 → INC 流程（OP-5），禁止跳过。

### 条例 FIX-07 · T 书 v2.1 修订落地（P9）
- 步骤：① 把《任务书T_执行条例版v2.1修订与下一步条例.md》（另一交付文件）复制入仓库 `docs/`（与 v2.0 同目录），并在任务书 T v2.0 文件头追加一行："**> v2.0 的 A3 序列/判据包/预算/OP-2 已由 v2.1 修订附录覆盖（2026-09-04），冲突处一律以 v2.1 为准**"；② workspace 的任务书 T v2.0 同样加该行（保持双副本一致）。
- Git 提交：`docs(t): T书 v2.1 修订附录入库 + v2.0 覆盖注记 [FIX-07]`
- 验收：✓ 仓库与 workspace 两处 v2.0 文件头都有覆盖注记；✓ v2.1 附录在 docs/。
- 卡住时：—

---

## 2. 完成门禁

1. 逐条例验收行打 ✓（本文件复制入仓库 `docs/` 后逐条勾）；
2. `git tag gate-FIX-20260904` + `git push origin gate-FIX-20260904`（OP-1 标签纪律）；
3. 账本登记一行：`2026-09-0x | FIX-01..07 | 0 GPU | 完成 | 进入 EX-01`；
4. 通知主智能体"FIX 全绿"，等待确认后启动 EX-01。

*版本 v1.0 · 2026-09-04 · 本册完成后 FIX 书作废归档（docs/archive/），不再复用。*

---

## 验收状态（执行方 2026-09-04 批注，供主智能体复核）

- FIX-01 ✓（README §4.1 世代双行/0.0532 命中=0/N曲线注记/R0 caption→10.66°）
- FIX-02 ✓（CLAIM_CARDS S-01 主行=Gen-A3 10.30°/32.55/0.1482 + reference-only 段；S-02/S-04 世代注记；禁词自检沿用）
- FIX-03 ✓（CLAIM_REGISTRY 版本头 v0.6→v0.7）
- FIX-04 ✓（G0 定稿判定 §六 + CALIBRATION 室温回填）
- FIX-05 ✓（run_card_howto 三指纹 + A3-0 RUN_CARD.json：code_commit_sha=unrecorded 诚实注记）
- FIX-06 ✓（run_arms 默认 bs4 + 冒烟 bs4 PASS/bs8 BLOCK）
- FIX-07 ⛔ **阻塞**：缺少交付文件《任务书T_执行条例版v2.1修订与下一步条例.md》（D:、workspace、仓库均未定位）——等待主智能体提供文件路径后落地。
- 门禁：gate-FIX-20260904 **未打标签**（FIX-07 未闭合）；账本登记"进行中"。

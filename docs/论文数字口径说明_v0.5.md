# 论文数字口径说明 v0.5(2026-09-07 · 卡W2)

> supersede v0.1(G0 裁决书可追加修订条款继续有效; 本版并入任务书 v3.3 §1 全部新锚点)。
> 适用范围:论文、答辩、demo、一切对外材料。v0.1 的 scene 级口径/世代纪律/指纹纪律全部沿用, 此处不重复。

## v0.5 新增锚点(v3.3 审计裁决)

### A. exp12v3 判 VOID(禁引旧数字)

| 项 | 规则 |
|---|---|
| ❌ 禁引 | exp12v3 的"MAE 0.09–0.5°"“7 迭代收敛”"0/4 显著"——**四处缺陷污染**(`任务书_增补v3.3 §1.1`): ①z 真值初始化(红线#1); ②档位 6/8 低于注册阶梯(n=6 临界|ρ|≥0.886, "≥3/4 显著"数学不可达); ③迹近规范方向污染(42.6→6.6e15); ④正则状态错配 |
| ✅ 保留 | `exp12v3_divergence_precheck`(40/40 残差比 8e10–7e16, 单步 z 更新 244–497× 场景尺度)——v2 发散直接观测证据, 永久有效 |
| 🔁 替代 | exp12v4(真值初始化修复+4 起点数据驱动+12/20 档+Λ 匹配迹+工作带 R-D5)——以新预注册三分支判读 |

### B. exp8R v3 主诊断两处代数错误(headline 污染)

| 项 | 规则 |
|---|---|
| ❌ 禁引 | **"0/10 正 6/10 负"headline**(被污染); `exp8r_verdict_v3.md` §3 "三假说全部排除 → 非实现缺陷"的跨对象外推句 |
| 缺陷 | ①ρ 边缘化按全局单标量口径实现(应逐像素对角精确 Schur); ②α 强度维完全未边缘化(docstring 声称与实现不符, 红线#9 由来) |
| ✅ 保留有效 | 估计器侧(trf+解析稀疏 Jacobian, FD 4e-9, 同最优解 4e-11)与 **LAE 响应数据**; exp8S 变体 A(严格 Schur+α 联合边缘化@真值)经主智能体逐行核验为正确实现 |
| 📌 当前最可信真实侧读数 | **变体 A: 4 正/3 负/3 ns**(混合); buddha/pot1/reading 严格 oracle 下仍显著负——残余负号真实且集中于高光物体;"偏差主导"假说(预注册压力轨预测形态)仍活着 |
| 🔁 替代 | exp8R v3.1(卡S2: 变体 A 代数移植 + 已知答案单测 + 拟合参数落盘 + 云端重跑) |

### C. SH-4 对照与破缺归因改写(卡X)

- **新锚点**:SH-2→SH-4 Rayleigh 比值 0.965/0.990/0.993(**降幅 <4%**)——"SH-2 截断是 GBR 破缺主源"的归因被自己的实验削弱;
- **谱分层句(新)**:"GBR 破缺量级(1e-3 带)由**附着阴影终止子非线性**主导, SH 截断贡献 <4%(SH-4 对照实测), 谱分层结构不依赖截断阶数"——**待卡X 定位实验坐实后方可引用**(富集 ≥5× 判据预写死); 若不集中 → 第三通道候选表如实列出;
- **旧句处置**:v3.1 §3 措辞 2 中"SH-2 截断破缺"段 = **待替换**(卡X 后定稿); exp9 三判别表同步;
- **有利定性**:1e-3 破缺是真实物理而非 SH-2 近似伪影(论文有利), 但在卡X 落地前**只可写"SH-4 对照显示截断贡献 <4%"**(实测事实), 不可写"终止子通道证实"(未坐实)。

### D. 红线 v3.3 增补(#8–#11, 方法论清单联动)

新增四条红线已并入 `docs/论文措辞包_v0.4.md §2`(D4 验证闸)引用范围:
已知答案单测(rel err<1e-10)、检查表第 5 行(边缘化声称≠实现, 以代码为准)、降档决策基于实际运行路径(LM 0.7s 后旧 trf 35min 数据作废)、GT 使用显式声明+assert。

**方法论清单增第 4 条(§4.3 v3.3)**:审计结论的范围必须与其检验对象逐一对应, 禁止跨对象外推(exp8S 教训: "三假说排除"检验的是简化式, 不是全部实现)。

## 引用速查(修订后唯一有效读数)

| 对象 | 唯一有效读数 | 状态 |
|---|---|---|
| 合成侧判别(支柱③) | **无(等 exp12v4)** | ⏸ 卡R2 运行中 |
| 真实侧判别(支柱③) | 变体 A 4正/3负/3ns(oracle@真值, 临时) | ⏸ 等 v3.1 修正@拟合值 |
| GBR 破缺主源 | SH-4 实测 <4%(截断贡献); 主源待卡X | ⏸ 卡X 插空 |
| v2 发散 | 残差比 8e10–7e16 ×40(直接观测) | ✅ 永久有效 |

---
*v0.5 · 2026-09-07 · 卡W2 落地。依据:`任务书_增补v3.3 §1/§4`; 禁词清单以 CLAIM_REGISTRY 为准。*
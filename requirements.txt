# Multi-Illumination Inverse Rendering - 运行依赖
# Python 3.8+，PyTorch 2.x（CUDA 版请按 https://pytorch.org 选择对应安装命令）
torch>=2.1
torchvision>=0.16
numpy>=1.24
Pillow>=10.0
tqdm>=4.60
matplotlib>=3.7
tensorboard>=2.14

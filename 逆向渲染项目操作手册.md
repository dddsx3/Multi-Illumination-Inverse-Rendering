# 逆向渲染项目操作手册

## 1. 项目概述

本项目是一个基于深度学习的逆向渲染系统，用于从多光照图像中分解出物体的深度图、反照率图和光照参数。系统使用U-Net架构进行内在图像分解，并通过物理渲染器重建图像，以监督模型学习。

### 核心功能
- 从多光照图像中分解深度图、反照率图和球谐光照系数
- 支持边缘感知渲染和方向光模型
- 包含层次化残差模块，用于处理非朗伯反射效应
- 实现三阶段课程学习策略，从几何学习到材质学习再到残差学习
- 提供完整的训练、测试和演示模式

## 2. 项目结构

```
ni_xuanran/
├── __pycache__/          # 编译后的Python文件
├── diagnosis_output/     # 诊断输出结果
├── health_reports/       # 训练健康检查报告
├── auto_moniter.py       # 自动监控脚本
├── auto_training_monitor.py  # 自动训练监控器
├── config.py             # 配置文件
├── data_loader.py        # 数据加载器
├── dataset_test.py       # 数据集测试脚本
├── inference.py          # 推理脚本
├── jian.py               # 诊断脚本
├── loss_functions.py     # 损失函数定义
├── main.py               # 主程序入口
├── physics_renderer.py   # 物理渲染器
├── residual_modules.py   # 残差模块
├── trainer.py            # 训练器
└── unet_model.py         # U-Net模型定义
```

### 核心文件说明
- **main.py**: 项目入口，支持训练、测试和演示模式
- **unet_model.py**: 定义U-Net架构，用于内在图像分解
- **trainer.py**: 实现训练逻辑，包括损失计算、模型保存等
- **physics_renderer.py**: 实现物理渲染，用于重建图像
- **data_loader.py**: 实现数据加载和预处理
- **loss_functions.py**: 定义各种损失函数
- **residual_modules.py**: 实现层次化残差模块

## 3. 环境要求

### 软件依赖
- Python 3.8+
- PyTorch 1.8+
- torchvision
- numpy
- PIL (Pillow)
- matplotlib (可选，用于可视化)

### 硬件要求
- GPU: NVIDIA GPU with CUDA support (推荐)
- 内存: 至少8GB RAM
- 显存: 至少4GB GPU memory (对于256x256图像)

## 4. 数据准备

### 数据格式
项目要求数据按照以下结构组织：
```
processed_data/
├── rgb/
│   ├── scene_000000/
│   │   ├── light_001.png
│   │   ├── light_002.png
│   │   ├── light_003.png
│   │   ├── light_004.png
│   │   └── light_005.png
│   ├── scene_000001/
│   │   └── ...
│   └── ...
```

### 数据要求
- 每个场景包含固定数量的光照图像（默认5张）
- 图像应为灰度图，分辨率建议为256x256
- 图像文件格式为PNG

### 数据预处理
数据加载器会自动对数据进行以下预处理：
- 调整图像大小到指定尺寸
- 转换为浮点数张量，范围归一化到[0, 1]
- 应用数据增强（仅在训练模式）

## 5. 模型架构

### U-Net 架构
- **编码器**: 4层下采样，提取多尺度特征
- **瓶颈层**: 特征融合
- **解码器**: 4层上采样，恢复空间分辨率
- **输出头**:
  - 深度头: 预测深度图 [B, 1, H, W]
  - 反照率头: 预测反照率图 [B, 1, H, W]，使用sigmoid激活
  - 权重图头: 预测自适应权重图 [B, 1, H, W]，使用sigmoid激活
  - 球谐头: 预测球谐光照系数 [B, K, 9]，使用tanh激活

### 物理渲染器
- 支持边缘感知渲染，保留物体边缘
- 支持方向光模型，模拟真实光照效果
- 输入：深度图、反照率图、球谐系数
- 输出：渲染图像、法线图、着色图

### 层次化残差模块
- 处理非朗伯反射效应
- 包含全局残差和局部残差
- 输入：反照率图、着色图、法线图、球谐系数
- 输出：最终渲染图像、全局残差、局部残差

## 6. 训练流程

### 三阶段课程学习
1. **第一阶段**: 几何学习
   - 重点学习深度图和光照参数
   - 强制反照率平坦，避免纹理泄漏
   - 约束光照均值，防止数值爆炸
2. **第二阶段**: 材质学习
   - 微调光照和材质参数
   - 引入反照率一致性约束
3. **第三阶段**: 残差学习
   - 启用残差模块，处理非朗伯反射
   - 微调所有参数，提高整体质量

### 训练步骤
1. **配置参数**: 在`main.py`中设置训练参数
2. **启动训练**: 运行`python main.py`
3. **监控训练**: 查看控制台输出和健康检查报告
4. **模型保存**: 训练过程中会自动保存检查点

### 训练配置
主要配置参数位于`main.py`中的`main()`函数内：
- `mode`: 运行模式，可选'train'、'test'或'demo'
- `data_root`: 数据根目录
- `num_lights`: 每个场景的光照图像数量
- `image_size`: 图像尺寸，格式为[高度, 宽度]
- `batch_size`: 批次大小
- `total_epochs`: 总训练轮数
- `learning_rate`: 学习率
- `stage1_epochs`: 第一阶段训练轮数
- `stage2_epochs`: 第二阶段训练轮数
- `base_channels`: 模型基础通道数
- `scheduler`: 学习率调度器，可选'step'、'cosine'或'plateau'
- `use_amp`: 是否使用混合精度训练
- `device`: 设备，可选'cuda'或'cpu'
- `aggressive_albedo_smooth`: 是否启用反照率平滑激进疗法
- `nuclear_deterrence`: 是否启用核威慑级别的残差惩罚
- `albedo_separation`: 是否启用反照率分离微调配置

## 7. 测试流程

### 测试步骤
1. **准备模型**: 训练完成后，在`checkpoints`目录中选择一个检查点
2. **配置参数**: 在`main.py`中设置`mode='test'`和`checkpoint_path`
3. **启动测试**: 运行`python main.py`
4. **查看结果**: 测试结果会保存在`output/test_results`目录中

### 测试输出
测试模式会为每个场景生成以下输出：
- 输入图像: `input_00.png`到`input_04.png`
- 渲染图像: `rendered_00.png`到`rendered_04.png`
- 最终渲染图像: `final_render_00.png`到`final_render_04.png`
- 深度图: `depth.png`
- 反照率图: `albedo.png`
- 权重图: `weight_map.png`
- 法线图: `normal_x.png`、`normal_y.png`、`normal_z.png`

## 8. 演示模式

### 演示步骤
1. **配置参数**: 在`main.py`中设置`mode='demo'`
2. **启动演示**: 运行`python main.py`
3. **查看结果**: 演示结果会打印到控制台，包括各输出的形状和数值范围

### 演示功能
- 加载一个场景的多光照图像
- 运行模型预测和渲染
- 打印各输出的详细信息
- 计算并打印损失值

## 9. 损失函数

### 主要损失函数
- **重建损失**: 衡量预测图像与真实图像的差异
- **深度平滑损失**: 保持深度图平滑
- **反照率平滑损失**: 强制反照率平坦，避免纹理泄漏
- **权重分布损失**: 约束权重图分布
- **权重总变差损失**: 保持权重图平滑
- **球谐L2损失**: 防止球谐系数数值爆炸
- **球谐高阶损失**: 鼓励使用高阶球谐系数
- **球谐稀疏性损失**: 鼓励球谐系数稀疏
- **反照率一致性损失**: 确保反照率在不同光照下一致
- **残差L1损失**: 约束残差大小
- **残差总变差损失**: 保持残差平滑

### 自定义损失函数
- **DepthVarianceLoss**: 强制深度图有足够的方差，避免平坦深度
- **ShadingMeanLoss**: 强制光照均值为1.0，防止数值爆炸
- **ShadingVarianceLoss**: 强制光照有足够的方差，避免均匀光照

## 10. 物理锁死机制

为了防止模型通过简单缩放环境光来解释纹理，项目实现了物理锁死机制：

### 实现位置
- **文件**: `unet_model.py`
- **位置**: `forward`方法中的物理约束应用部分

### 核心代码
```python
# 🔴【关键修改】物理锁死：环境光不得大于 1.0
# 这就废掉了 SH[0] 的"增益放大器"功能
# 模型被迫使用高阶系数（SH Higher）来解释纹理和暗角
# 使用非原地操作，避免破坏梯度计算
sh0_clamped = torch.clamp(sh_coeffs[:, :, 0:1], 0.0, 1.0)
sh_higher = sh_coeffs[:, :, 1:]
sh_coeffs = torch.cat([sh0_clamped, sh_higher], dim=-1)
```

## 11. 训练健康检查

### 健康检查报告
- **文件位置**: `health_reports/`目录
- **命名格式**: `health_report_epoch_{epoch}.txt`
- **内容**: 包括各输出的统计信息、损失值、梯度范数等

### 诊断功能
- **几何状态检查**: 评估深度图和法线图的质量
- **材质状态检查**: 评估反照率图的平滑度
- **光照状态检查**: 评估光照参数的合理性
- **训练稳定性检查**: 监测梯度范数和损失值的变化

## 12. 常见问题与解决方案

### 1. 内存不足错误
- **错误信息**: `CUDA out of memory`
- **解决方案**:
  - 减小批次大小 (`batch_size`)
  - 减小图像尺寸 (`image_size`)
  - 启用混合精度训练 (`use_amp=True`)

### 2. 梯度计算错误
- **错误信息**: `one of the variables needed for gradient computation has been modified by an inplace operation`
- **解决方案**:
  - 确保所有操作都是非原地的，避免使用`inplace=True`
  - 检查`unet_model.py`中的物理锁死代码，确保使用非原地操作

### 3. 训练不稳定
- **症状**: 损失值波动大，或出现NaN
- **解决方案**:
  - 减小学习率 (`learning_rate`)
  - 增加权重衰减 (`weight_decay`)
  - 检查数据加载器，确保数据范围正确
  - 启用梯度裁剪 (`gradient_clip`)

### 4. 反照率泄漏
- **症状**: 反照率图包含过多纹理
- **解决方案**:
  - 增加`albedo_smooth`权重
  - 启用`aggressive_albedo_smooth`
  - 检查第一阶段配置，确保反照率平滑约束足够强

### 5. 光照数值爆炸
- **症状**: 球谐系数过大，渲染图像过亮
- **解决方案**:
  - 增加`sh_l2`和`shading_mean`权重
  - 检查物理锁死代码，确保SH[0]被正确约束
  - 减小学习率，特别是对于球谐系数头

## 13. 代码修改指南

### 修改模型架构
- **文件**: `unet_model.py`
- **修改点**:
  - `IntrinsicUNet`类的`__init__`方法: 修改网络结构参数
  - `forward`方法: 修改前向传播逻辑
  - `_initialize_weights`方法: 修改权重初始化策略

### 修改损失函数
- **文件**: `loss_functions.py`
- **修改点**:
  - 添加新的损失函数类
  - 修改`LossCalculator`类的`__call__`方法，集成新的损失函数

### 修改训练配置
- **文件**: `trainer.py`
- **修改点**:
  - `_define_stage_configs`方法: 修改各阶段的损失权重
  - `_setup_optimizer`方法: 修改优化器配置
  - `_setup_scheduler`方法: 修改学习率调度器配置

### 修改数据加载
- **文件**: `data_loader.py`
- **修改点**:
  - `MultiLightingDataset`类的`__init__`方法: 修改数据加载参数
  - `__getitem__`方法: 修改数据预处理逻辑
  - `_load_grayscale_image`方法: 修改图像加载和处理逻辑

### 修改物理渲染
- **文件**: `physics_renderer.py`
- **修改点**:
  - `PhysicsRenderer`类的`__init__`方法: 修改渲染参数
  - `forward`方法: 修改渲染逻辑

## 14. 输入输出示例

### 输入示例
```python
# 输入: 5张光照图像，形状为[B, K, H, W]
# 例如: [2, 5, 256, 256]
# 数值范围: [0, 1]
```

### 输出示例
```python
# 输出1: 深度图，形状为[B, 1, H, W]
# 例如: [2, 1, 256, 256]
# 数值范围: 任意正值

# 输出2: 反照率图，形状为[B, 1, H, W]
# 例如: [2, 1, 256, 256]
# 数值范围: [0, 1]

# 输出3: 球谐系数，形状为[B, K, 9]
# 例如: [2, 5, 9]
# 数值范围: [-1, 1]，但SH[0]被约束在[0, 1]

# 输出4: 权重图，形状为[B, 1, H, W]
# 例如: [2, 1, 256, 256]
# 数值范围: [0, 1]

# 渲染输出: 渲染图像，形状为[B, K, H, W]
# 例如: [2, 5, 256, 256]
# 数值范围: [0, 1]

# 法线输出: 法线图，形状为[B, 3, H, W]
# 例如: [2, 3, 256, 256]
# 数值范围: [-1, 1]

# 着色输出: 着色图，形状为[B, K, H, W]
# 例如: [2, 5, 256, 256]
# 数值范围: [0, 1]
```

## 15. 性能优化建议

### 内存优化
- **启用混合精度训练**: 设置`use_amp=True`
- **使用梯度累积**: 对于大批次，使用梯度累积模拟大批次训练
- **减少中间变量**: 优化前向传播，减少不必要的中间变量

### 速度优化
- **使用CUDA流**: 重叠数据传输和计算
- **启用CUDA图**: 对于固定输入大小，使用CUDA图加速
- **优化数据加载**: 使用`num_workers>0`和`persistent_workers=True`

### 模型优化
- **模型剪枝**: 移除不重要的参数
- **量化**: 考虑使用INT8量化，加速推理
- **知识蒸馏**: 使用大模型指导小模型学习

## 16. 总结

本项目实现了一个完整的基于深度学习的逆向渲染系统，从多光照图像中分解出物体的深度图、反照率图和光照参数。系统采用U-Net架构进行内在图像分解，结合物理渲染器和层次化残差模块，实现了高质量的逆向渲染效果。

通过三阶段课程学习策略和各种约束损失函数，系统能够有效地学习几何形状、材质属性和光照条件。物理锁死机制进一步确保了物理正确性，防止模型通过简单缩放环境光来解释纹理。

项目提供了完整的训练、测试和演示功能，以及详细的健康检查和诊断工具，方便用户监控训练过程和调试问题。通过本操作手册，新人应该能够快速了解项目结构和使用方法，开始进行逆向渲染相关的研究和开发。
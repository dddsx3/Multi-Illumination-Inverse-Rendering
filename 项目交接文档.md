# 逆向渲染项目 - 交接文档

**项目名称**: 多光照图像逆向渲染系统
**创建日期**: 2026-01-22
**交接日期**: 2026-05-22
**项目状态**: 已完成核心功能开发，待业务交接

---

## 一、项目概述

### 1.1 项目目标

本项目实现了一个**多光照图像逆向渲染系统**，能够从5张不同光照条件下的灰度图像中恢复出场景的：
- **深度图** (Depth Map)
- **反照率图** (Albedo)
- **法线图** (Normal Map)
- **球谐光照系数** (Spherical Harmonics Coefficients)
- **权重图** (Weight Map)

### 1.2 核心技术路线

```
输入: 5张多光照灰度图像
  ↓
[IntrinsicUNet] → 深度、反照率、球谐系数、权重
  ↓
[PhysicsRenderer] → 深度→法线(Sobel) → 球谐光照计算 → 渲染图像
  ↓
[HierarchicalResidual] → 非朗伯效应修正
  ↓
输出: 分解后的内在属性 + 重建图像
```

### 1.3 技术架构

| 层级 | 模块 | 说明 |
|-----|------|------|
| 模型 | `unet_model.py` | IntrinsicUNet，多输出头U-Net架构 |
| 渲染 | `physics_renderer.py` | 可微分物理渲染器（深度→法线→SH光照） |
| 残差 | `residual_modules.py` | 分层残差模块，处理非朗伯效应 |
| 损失 | `loss_functions.py` | 多目标损失函数（重建+平滑+先验） |
| 训练 | `trainer.py` | 三阶段课程学习训练器 |
| 数据 | `data_loader.py` | 多光照数据集加载器 |

---

## 二、目录结构

```
n_i_xuanran/
├── main.py                    # 主程序入口（训练/测试/演示）
├── config.py                  # 配置管理
├── unet_model.py              # U-Net模型
├── physics_renderer.py         # 物理渲染器
├── residual_modules.py         # 残差模块
├── loss_functions.py           # 损失函数
├── trainer.py                  # 训练器
├── data_loader.py              # 数据加载器
├── inference.py                # 推理脚本
├── auto_training_monitor.py    # 训练监控
│
├── 交付文件夹/                  # 可交付物
│   ├── best_model.pth         # 训练好的模型权重
│   ├── 逆渲染GUI.py           # GUI推理工具
│   ├── 使用说明.md            # 使用文档
│   └── [核心模块副本]
│
├── ice_root/                   # 冰层仿真器模块（独立项目）
│   ├── configs/                # 配置文件
│   ├── scripts/               # 运行脚本
│   └── src/
│       ├── physics/           # 物理方程（波方程、谱求解器、激光源）
│       ├── models/            # 神经网络模型
│       ├── data/              # 数据生成与预处理
│       └── utils/             # 工具函数
│
└── Kimi_Agent_仿真器参数可视化/  # 仿真器参数调试工具
    ├── ice_simulator.py       # 冰层仿真器
    ├── spectral_solver.py     # 谱求解器
    ├── ice_trainer.py         # 仿真器训练器
    ├── simulator_gui.py      # 仿真器GUI
    └── [其他调试工具]
```

---

## 三、核心模块详解

### 3.1 IntrinsicUNet (`unet_model.py`)

**功能**: 多输出头U-Net，同时预测深度、反照率、球谐系数、权重

**输出**:
- `depth`: [B, 1, H, W] 深度图
- `albedo`: [B, 1, H, W] 反照率图
- `sh_coeffs`: [B, K, 9] 球谐系数（K=5光照数，9个二阶SH基）
- `weight_map`: [B, 1, H, W] 自适应权重图

### 3.2 PhysicsRenderer (`physics_renderer.py`)

**功能**: 可微分物理渲染管线

**渲染流程**:
1. **DepthToNormal**: 使用Sobel算子从深度图计算法线
2. **SphericalHarmonicsLighting**: 二阶球谐光照（9个基函数）
3. **渲染方程**: `rendered = albedo × shading`

**关键特性**:
- 边缘感知的法线计算
- 支持多光照场景
- 端到端可微分

### 3.3 HierarchicalResidual (`residual_modules.py`)

**功能**: 处理非朗伯效应（全局残差+局部残差）

**输入**: albedo, shading, normal, sh_coeffs
**输出**: final_render, global_residual, local_residual

### 3.4 三阶段课程学习 (`trainer.py`)

| 阶段 | 名称 | 重点 | Epochs |
|-----|------|------|--------|
| Stage 1 | 几何学习 | 深度、光照分离，Albedo粉碎 | 30 |
| Stage 2 | 材质学习 | 反照率、权重正则化 | 30 |
| Stage 3 | 残差学习 | 非朗伯效应建模 | 后续 |

**达标标准**（连续10个epoch满足）:
- Shading Variance > 0.01
- SH[0] Mean ∈ [0.8, 1.2]
- Albedo Correlation < 0.4
- Lambertian Ratio > 0.95

---

## 四、数据规格

### 4.1 数据集结构

```
数据根目录/
├── rgb/                      # 场景集合
│   ├── scene_000000/
│   │   ├── light_001.png
│   │   ├── light_002.png
│   │   ├── light_003.png
│   │   ├── light_004.png
│   │   └── light_005.png
│   ├── scene_000001/
│   └── ...
├── rgb_1/
│   └── ...
└── rgb_2/
    └── ...
```

### 4.2 数据参数

| 参数 | 默认值 |
|-----|--------|
| 光照数量 (K) | 5 |
| 图像尺寸 | 256×256 |
| 批量大小 | 8 |
| 文件扩展名 | .png |

---

## 五、配置参数

### 5.1 训练配置 (`config.py`)

```python
# 训练参数
total_epochs: 100
learning_rate: 1e-4
weight_decay: 1e-6
scheduler: 'step'  # step/cosine/plateau/none

# 模型参数
base_channels: 32
sh_order: 2  # 二阶球谐
num_images: 5

# 损失权重
reconstruction: 1.0
depth_smooth: 0.01
albedo_smooth: 0.01
```

---

## 六、使用方法

### 6.1 训练模型

```bash
python main.py --mode train
```

或在 `main.py` 中修改配置后直接运行。

### 6.2 测试模型

```bash
python main.py --mode test --checkpoint 交付文件夹/best_model.pth
```

### 6.3 GUI推理

```bash
cd 交付文件夹
python 逆渲染GUI.py
```

### 6.4 命令行推理

编辑 `inference.py` 中的路径配置后运行。

---

## 七、交付物清单

| 文件 | 说明 | 位置 |
|-----|------|------|
| `best_model.pth` | 训练好的模型权重 | `交付文件夹/` |
| `逆渲染GUI.py` | GUI推理工具 | `交付文件夹/` |
| `使用说明.md` | 用户使用文档 | `交付文件夹/` |
| 核心模块副本 | 推理所需的Python模块 | `交付文件夹/` |

---

## 八、技术债务与待优化项

### 8.1 已知的实验性配置

1. **残差模块冻结**: 当前训练中将残差模块冻结（`param.requires_grad = False`），迫使主网络学习纯物理渲染
2. **Albedo粉碎机**: Stage 1使用50.0的高权重强制平滑反照率
3. **激进疗法**: `aggressive_albedo_smooth` 标志启用自适应权重调整

### 8.2 潜在问题

1. 模型对数据分布敏感，需要相同格式的5张多光照图像
2. 球谐阶数固定为2阶，不支持更高阶
3. 残差模块在某些配置下被禁用

### 8.3 优化建议

1. 增加更多光照数量支持（当前固定K=5）
2. 添加更多损失项的可配置性
3. 考虑添加真值深度监督
4. 模型量化以支持边缘设备部署

---

## 九、相关项目说明

### 9.1 ice_root（冰层仿真器）

独立的物理仿真项目，包含：
- 波传播方程求解
- 激光-冰层相互作用
- 谱方法求解器

### 9.2 Kimi_Agent_仿真器参数可视化

仿真器调试工具集，包含：
- 参数采样与可视化
- 噪声生成器
- 数据增强管道
- 不确定性量化

**注意**: 这些工具与逆向渲染主项目关联有限，可能是前期探索性工作。

---

## 十、联系方式与参考

- **项目作者**: Python Engineer
- **创建日期**: 2026-01-22
- **最后更新**: 2026-01-24

---

## 附录：关键文件快速索引

| 功能 | 文件 | 关键类/函数 |
|-----|------|------------|
| 模型定义 | `unet_model.py` | `IntrinsicUNet`, `DownBlock`, `UpBlock` |
| 物理渲染 | `physics_renderer.py` | `PhysicsRenderer`, `DepthToNormal`, `SphericalHarmonicsLighting` |
| 残差模块 | `residual_modules.py` | `HierarchicalResidual` |
| 损失函数 | `loss_functions.py` | `LossCalculator`, `CharbonnierLoss`, `DepthSmoothnessLoss` |
| 训练器 | `trainer.py` | `InverseRenderTrainer` |
| 数据加载 | `data_loader.py` | `MultiLightingDataset` |
| 配置管理 | `config.py` | `Config`, `DataConfig`, `ModelConfig` |
| GUI工具 | `交付文件夹/逆渲染GUI.py` | `InverseRenderGUI` |
